// picovtuber-core - 非公開の内部共通コア。
//
// デスクトップアプリの枠組み・OS・製品設定に依存しない処理だけを置く。ここへ製品固有の
// 依存（ウィンドウ枠組みのAPI、設定キーの名前空間、アプリ側の設定モジュール）を
// 持ち込まないことは、アプリ側の `guard_scan::guard` のメタガードが検査する。
//
// 分けている理由は行数ではなく、**外部依存の境界**。ここに置いたものは、アプリを起動
// しなくても単体で検証でき、口形と表情の値域のような「間違えると画面が壊れる規則」を
// 実装の1箇所に閉じ込められる。

pub mod expression;
pub mod text;
pub mod viseme;
