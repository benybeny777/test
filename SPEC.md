# SPEC.md — LocalVTuberStudio 設計仕様

イラスト1枚から原画の見た目を保った2D/2.5D VTuberモデルを生成し、まばたき・口パク・小角度モーションを付けるデスクトップアプリ。**クラウドAPIを一切使わず、すべてローカルで完結する。** 品質不合格だった3D/VRM経路は廃止し、新しい実装へ持ち込まない。

- 恒久ルールは [AGENTS.md](AGENTS.md)。未完了作業と着手順は [docs/TASKS.md](docs/TASKS.md)。
- このファイルは**現行仕様の正本**。実装が変わったら同じ作業内でここを更新する。

製品名 `LocalVTuberStudio` / 識別子 `com.localvtuberstudio.desktop` は**確定**（2026-09-02 利用者決定）。

## 1. 何を作るか

| 機能 | 本家 | 本実装 |
|---|---|---|
| イラスト1枚 → 動くモデル | Meshy（クラウド） | 原画を意味レイヤーへ分解するローカル2.5D方式 |
| 自動リギング | Meshy（クラウド） | **テンプレートヒューマノイド適合＋自動スキニング**（下記4.3） |
| 表情生成（6種＋任意） | gpt-image-2（クラウド） | ローカル拡散モデルの顔インペイント |
| 母音口形（あいうえお＋閉） | gpt-image-2（クラウド） | 同上 |
| 表情・口パク | フェイスパッチ投影 | 2.5Dレイヤーの目・口を `MouthOpenY` と `MouthForm` で変形 |
| リップシンク | ローカル（フォルマント） | 同方式を独自実装（下記4.5） |
| 音声認識 | ローカル（whisper.cpp） | whisper.cpp |
| 会話LLM | ローカル（llama.cpp） | llama.cpp |
| 背景生成 | クラウド | ローカル拡散モデル |
| OBS出力 | MJPEG＋グリーンバック必須 | **透過ブラウザソース直描画**（本家を上回る。下記4.6） |
| 表情・動画の自動選択 | ローカル小型LLM（`qwen2.5-1.5b` でJSON選択） | 同方式。**生成ではなく選択タスクとして設計する** |
| 動画オーバーレイ合成 | 透過動画を3D画面へクロマキー合成 | **合成機能は実装する**（下記4.8）。素材は利用者持ち込みでよい |
| 動画の生成 | クラウド（BytePlus） | **初期スコープ外**（8GB VRAMで実用にならない） |
| 課金・認証 | あり | **なし** |

## 1.1 背景 — PicoAgent のキャラクター制作（参考情報）

**本プロダクトの本来の目的は PicoAgent（`C:\00_PG\20_PicoAgent`）向けのキャラクター制作**（2026-09-02 利用者より）。利用者自身が制作して導入する形にするか、PicoAgent の利用者が自分で作れる形にするかは、**動作を見てから決める**。

現時点では**背景情報であり、要件ではない**。この目的のために本仕様を作り替えない。ただし次の2点は設計判断へ影響するため、最初から外さないようにする。

1. **技術スタックを PicoAgent（Tauri + Rust + webview）へ寄せる。** 将来統合する場合の摩擦を減らす。これが本プロジェクトで Tauri を選んだ理由の1つ
2. **Python 環境を増やさない**（下記2の方針5）。PicoAgent は既に THA3 用の Python ランタイムを同梱しており、こちらが ComfyUI 用と3D生成用を別々に持つと、統合時に**3つの Python 環境**が並ぶ

**キャラクター形式は PicoAgent の既存形式に合わせない**（2026-09-02 利用者決定）。PicoAgent の現行キャラは `characters/<id>/` 配下の2D資産（レイヤー画像・連番フレーム・Live2D）だが、**本プロジェクトに最適な新形式を設計し、PicoAgent 側が後からその形式に対応する**方針。

したがって 5.1 のキャラクター保存形式は、**PicoAgent への互換性ではなく本プロジェクトの都合で決めてよい**。既存形式へ寄せるための妥協をしない。

要件化するかどうかは利用者が動作を見て判断する。**エージェント側から先回りして PicoAgent 連携を実装しない。**

## 2. 基本方針

1. **クラウドAPIを呼ばない。** ネットワークアクセスはモデルの初回ダウンロードだけに限る。生成中は完全オフラインで動く。この不変条件はテストで機械検査する。
2. **本家のコード・アセット・プロンプト文言を複製しない。** 参照するのは動作仕様とデータ構造のみ。
3. **VRAM 8GB で実際に動くことを最優先する。** 動かない構成を「将来のため」に採用しない。
4. **利用者に Python のインストールを要求しない。** 必要な工程には Python ランタイムを同梱する。
5. **同梱する Python 環境は1つだけにする。** ComfyUI・画像→3D・リギングは**同一の Python 3.12 環境を共有**する。用途ごとに環境を分けない。PicoAgent が既に THA3 用ランタイムを同梱しており（1.1）、将来統合したときに Python 環境が乱立するのを防ぐ。依存が衝突する場合は、環境を増やすのではなく**版を揃えて解決する**。どうしても解決できない場合だけ、理由を明記して利用者へ相談する。
6. **失敗を黙って隠さない。** 生成が落ちたら理由を利用者に見せる。無言フォールバック禁止（[AGENTS.md](AGENTS.md)）。

## 3. 技術スタック

| 層 | 採用 | 理由 |
|---|---|---|
| アプリ本体 | **Tauri 2 + Rust** | 配布が軽い。プロセス管理・ファイル・HTTPをRustで持てる |
| 操作UI | **webview（HTML/CSS/JS）** | Tauri標準。Node.js をアプリ依存にしない |
| 3D描画 | **three.js**（`ui/` と `ui-stream/` で共有） | OBSブラウザソースが同じコードで動く。これが4.6の前提 |
| 画像生成・編集 | **ComfyUI 同梱**（4.7.2） | ControlNet・IP-Adapter・インペイントが揃い、表情生成の品質リスクを下げられる。**品質優先の決定** |
| 音声認識 | **whisper.cpp** | 本家と同じ。Python不要 |
| 会話LLM | **llama.cpp** | 本家と同じ。Python不要 |
| マイク入力・信号処理 | **Rust（cpal + rustfft）** | 解析を1箇所に集約し、UIとOBSの両方へ配信する（4.5） |
| **画像→3D** | **Python 3.12 同梱 + PyTorch** | ここだけ代替が存在しない |
| **リギング** | **Python 3.12 同梱**（PyTorch不要の見込み。4.3） | 同上 |
| 開発タスク | `cargo xtask` | Node.js をビルド依存にしない |

`cargo` と `Tauri CLI`、Windows は WebView2 ランタイムが必要。**Node.js はアプリの依存にしない**（作業用ツールとしての利用は可）。

### 3.1 Python サイドカーの境界

ComfyUI を採用した結果、**Python は画像・3D・リギングの3経路に入る**。当初「Python は3D生成とリギングの2工程だけ」を目標にしていたが、**品質を優先して範囲を広げる判断をした**（2026-09-02 利用者決定）。代償として配布サイズと起動時間が増える。

Python プロセスは2種類あり、**扱いが異なる**。

| 種別 | 対象 | 形態 | 理由 |
|---|---|---|---|
| **常駐** | ComfyUI | 起動しっぱなしのローカルHTTPサーバ | モデルのロードが重く、毎回起動すると実用にならない |
| **単発** | 画像→3D、リギング | 1回ごとにプロセスを起動して終了 | 実行頻度が低く、常駐させるとVRAMを占有し続ける |

共通の規則:

- 起動と停止は Rust 側の `sidecar` モジュールが一元管理する。**UI やスキルから直接起動しない**
- **子プロセスは終了時に `kill` し `wait` でハンドルまで回収する**（[AGENTS.md](AGENTS.md)）。ComfyUI は子プロセスを自分で生やすため、プロセスグループごと回収する
- 進捗は stdout の JSON Lines（ComfyUI は WebSocket）で受け、Rust が UI へ中継する
- **VRAM を取り合わないよう、常駐ComfyUIと単発の3D生成を同時に走らせない。** どちらかを実行する前に他方のモデルをアンロードする
- 単発側は入出力をファイルパスと JSON 1行でやり取りする

#### ComfyUI 固有の規則

- **同梱して本アプリ管理下で起動する。利用者に別途インストールを要求しない**
- **利用者の既存 ComfyUI 環境（設定・モデル・カスタムノード）を読まない・書かない。** 同名の環境変数やポートを奪わない
- **開発機の手元 ComfyUI に依存する実装にしない。** 開発者の環境で動いて利用者の環境で動かない、が最も起きやすい失敗
- ワークフローは JSON としてリポジトリで管理し、**使うカスタムノードを固定版で同梱する**。ノードの更新でワークフローが壊れるため、自動更新しない
- ポートは競合を避けて動的に選び、**利用者の既存 ComfyUI（既定8188）と衝突させない**

## 4. アーキテクチャ

### 4.1 フォルダ構成

```
src-tauri/          Rust本体
  src/
    config.rs       設定の正本（永続ファイル app_config_dir/config.json）
    pipeline.rs     生成パイプラインの工程定義と進行管理
    facepatch.rs    フェイスパッチ投影（4.4）— 本実装の中核
    lipsync.rs      マイク解析・母音判定（4.5）
    stream.rs       OBS配信用HTTPサーバ + WebSocket（4.6）
    sidecar.rs      Pythonプロセス制御
    engines.rs      whisper.cpp / llama.cpp の起動と対話
    store.rs        状態ファイルの原子的書き込み
ui/                 操作UI（webview）
ui-stream/          OBSブラウザソース用ページ（UIなし・透過）
  shared/           ui と ui-stream で共有する three.js 描画コード
sidecar/            Python 3.12
  background/       ComfyUIによるローカル背景生成
  expression/       表情・口形生成と外部画像検査
  mesh/             背景除去・画像→3D・UVアトラス生成
  rigging/          テンプレート適合＋自動スキニング
docs/               SETTINGS.md / DEVELOPMENT.md / TASKS.md
xtask/              開発タスク（cargo xtask）
temp/               一時作成物のみ。.gitignore 済み
```

### 4.2 生成パイプライン

現行の意味解析は承認済みGrounding DINO base→SAM2を逐次実行する。`analysis/analysis.json`と`analysis/masks.npz`に候補・選別・原寸マスクを保存し、入力画素、モデルファイル、解析コード、閾値、torch/Transformersの署名が一致する場合だけ再使用する。素材補完だけの変更では意味解析のGPU処理を繰り返さない。レイヤーmanifestは版2、リグは版3で、`texture_box`に切詰めPNGの原寸配置領域を保存する。描画前に実寸と領域の一致を検査し、版2リグは再生成を求める。これは機能単位分割の完成を意味しない。

#### 品質優先パイプラインの設計

**到達設計と現行試作を区別する。** 現在実行できる3工程は検証用であり、以下の設計は未実装部分を含む。SAM候補・幾何的な切り分け・単一メッシュだけで高品質な自動素材制作が成立する、という前提を置かない。

| 工程 | 入力 → 保存する成果物 | 次へ進む条件 |
|---|---|---|
| 1 原画保持・透過検査 | 原画 → 不変のsource/input.png、alpha、原寸の中立画像 | 人物の欠損・ハローを検査。透過があるだけで背景除去済みと断定しない |
| 2 構造解析 | 中立画像 → analysis.json（可視/遮蔽/画面外、顔ランドマーク、関節、部位候補、信頼度） | SAMを意味分類器と見なさず、髪と顔・首と襟等の競合を解決。未知を固定位置で埋めない |
| 3 素材分離 | 構造情報＋原画 → 分割PNG/PSD、part_graph.json、原画画素の対応表 | 元の見えている画素を保持し、重複・欠落・誤分類を検査。機能単位を満たす |
| 4 隠れ部分の補完 | 分割素材＋遮蔽関係 → completed_parts、補完マスク、候補別検証結果 | 見えていた領域を無断で描き替えず、動作で露出する顔・白目・首・肩等を補完。補完未達を穴や単色塗りで隠さない |
| 5 リグとパラメータ | 素材＋part_graph → rig、部位別メッシュ、親子変形、クリップ、物理設定 | 目・口の独立構造と首/襟・肩/袖の接続を使う。単一平面変形を独立リグと呼ばない |
| 6 自動動作検査＋目視 | 同じrig/renderer → 原画比較・静止/連続動作・素材単独表示・診断 | 構造検査、画像検査、利用者の品質承認を別状態で保持。工程終了だけで完成にしない |

全工程で入力ハッシュ・コード/モデル/設定の版・出力ハッシュ・座標変換・実行条件を保存する。上流変更時は依存する工程のみ再実行し、原画と利用者承認済み資産を先に破棄しない。新出力はstagingで検証してから原子的に切り替える。GPUの同時実行を避け、バッチ/オフロードで速度とメモリを調整するが、解像度や素材単位を無断で減らさない。

`part_graph` は安定したinstance_idと機能roleを分ける。髪束・装飾は同じroleの複数instanceを許し、原画にない素材・画面外・遮蔽で見えない素材を区別する。必須部位は観測した構図と必要な動きから決め、全入力に同じ枚数を要求しない。親子・描画順・クリップ先・支点・接続領域・可視マスク・補完マスクを持つ。

**未確定の中核:** 信頼できる意味/ランドマーク解析と、原画の画風を維持した局所補完の自動化方式。現在の暗線探索・SAM候補だけでは汎用性が確認できていない。既存承認済みローカル経路で比較し、別モデルが必要なら品質・実機成立・権利を比較して利用者へ提示する。候補を勝手に採用/降格しない。設計上の空欄を「後で調整すれば動く」で埋めない。

受け入れ検査は同一処理・同一設定で実写/女性A/むぎを通すことに加え、未知の立ち絵を保留検証用に1枚以上用いる。原画一致、閉口線、開口2軸の格子点と連続往復、左右別の半閉/閉眼、首・襟・肩の小角度往復、白黒市松背景、原寸顔クロップ、全身、キャラ切替時のメモリを検査する。しきい値は測定・校正前に成功条件として捏造せず、実画面の欠陥と対応づけて確定する。

#### 素材分割の到達条件

リグ作成前に全レイヤーの形式版、部位名の重複、パスのディレクトリ内制約、PNG/RGBA形式、キャンバス寸法、非空アルファ、bbox・pivot・重なり順を検証する。分解・リグは出力先の親の`temp/`内へ全素材を生成・同期してから公開し、生成失敗時は旧出力を維持する。同一出力の二重生成をOSファイルロックで拒否し、公開rename失敗は旧版へ戻す。旧版の退避直後にプロセスが終了した場合は次回生成開始時に復旧する。背景除去PNGは同期後に単一ファイルを原子的置換する。工程開始時に下流の完了状態は失効させるが、成果物を先に削除しない。

この保存保護は目視合格の証明ではない。またディレクトリの二段階renameには短い未配置期間があるため、表示中の読者まで保護する原子的な世代参照切替とは区別する。異常終了後、次の生成を実行するまでの自動復旧と、読み込み中の世代固定は未実装。

利用者の追加要件として、Live2D向け素材制作と同程度の機能単位を目標にする。14レイヤーの試作はこの条件を満たさず、単一平面の連続変形を最終到達点にしない。Cubism SDK採用やmoc3出力の決定とは別であり、実行形式は引き続き製品内2.5Dで検証する。

| 領域 | 必要な独立素材 | 接続・補完の条件 |
|---|---|---|
| 顔 | 顔の下地、左右眉、鼻、左右耳 | 目口を外した下地、髪の下の顔、耳の付け根を補完 |
| 左右の目 | 各白目・虹彩/瞳・ハイライト・上まつげ/まぶた・下まぶた | 白目で瞳をクリップし、上下まぶたで閉じる。閉眼PNGの矩形重ねだけで代用しない |
| 口 | 上唇/上口線、下唇/下口線、口内、必要な歯・舌 | 閉口線を保持し、口内をクリップ。原画の口との二重描画を防ぐ |
| 首・服 | 首、首の影、胴体、襟、胸元の装飾 | 首を襟の後ろへ延長して補完。服を顔レイヤーへ混ぜず、首下の矩形・切断面を出さない |
| 髪 | 前髪・左右横髪・後髪を動かす束ごとに分割 | 被覆範囲を補完し、束の付け根と描画順を保存 |
| 腕 | 左右の上腕・前腕・手、独立して動く袖 | 肩・肘・手首を接続し、回転時の露出部分を補完 |

分割数は一律に固定しない。原画にない歯・装飾などは存在しないことを明示し、空PNGや同一画像の複製でパーツ数を満たさない。各素材に親子関係、描画順、クリッピング先、変形支点、元画像上の領域、補完領域と生成根拠を保持する。画像名・キャラIDで分岐せず、同一処理・同一設定で実写・女性A・むぎを検証する。

資料: [Live2D公式・イラストの加工](https://docs.live2d.com/cubism-editor-tutorials/psd/)、[素材分けについて](https://docs.live2d.com/cubism-editor-manual/divide-the-material/)。公式素材・コードを転載せず、分割の考え方を参照する。

現行2.5Dの描画は原画中立レイヤーと目口差分を同じキャンバスへ合成し、単一の連続メッシュへ貼る。独立した顔・腕の矩形を回転させない。閉口時は原画をそのまま使い、開口時だけ原画の実測位置へ下地補完と口腔を描く。閉眼は周囲の境界画素を保持した局所補完と原画の線色から生成する。実写にアニメ的な線が合うかを含め、品質は未達・検証中である。

`layers/manifest.json` は `schema_version: 2`、`rig2d/rig.json` は原寸切詰め画像の`texture_box`を持つ `schema_version: 3`。原画の画素とアルファを完全保持する `neutral` を含む18必須素材と、検出できた首・襟を保存する。各部位に `bbox`、実測正規化 `pivot`、目口に `feature_box`、生成差分に `line_color` を記録する。目口の検出不能を固定比率で補わず明示失敗にする。描画は中立・口下地・口原画と左右各3素材（目の原画・肌の下地・上まつげ）の9枚を画像展開し、その他の部位は位置メタデータを使う。

まばたきは`ui/shared/eye-geometry.js`を本体と確認画面で共有する。原画から上まつげの色・太さ・輪郭と目の下端を測定し、`eye_aperture`に列ごとの原画座標`[x, 上端, 下端, 閉眼位置]`を保存する。肌の下地、上まつげを独立画像にし、瞳を縦につぶしたり透明度で二重表示したりせず、上下境界によるクリップと上まつげの移動で閉じる。`eye_rig_version: 1`を必須とし、境界の順序・有限値・キャンバス内・素材間一致をリグ生成前に検査する。原画の白目/虹彩/下まぶたの独立素材化は未完了である。

口は`ui/shared/mouth-geometry.js`を本体・ブラウザで共有する。分解時に口の局所コントラストと連続性から合わせ目を追跡し、原画座標の`mouth_closed.lip_seam`として保存する。`mouth_closed`には周囲の肌を含む原寸画像を保存し、上下のメッシュはこの線で分割する。`MouthOpenY × MouthForm`で合わせ目側を開閉・変形し、外周を元の位置に固定する。中立時は原画をそのまま描き、原画にない唇を手続き的に塗り直さない。リグは`lip_rig_version: 1`を必須とし、古いリグは分解から再生成する。

原画の上下唇の質感を保持するが、口内と舌はまだ手続き描画の試作で、歯など隠れた内部の素材は未完成。以前のBézier塗り口は比較用コードとして残すが既定描画では使用しない。口の上下はCanvas内で独立メッシュとして変形後に合成している段階で、キャラ全体のThree単一平面をPico/Animeの独立部位構造へ置換したわけではない。確認ページの無音口パクテストは本体と同じ母音プリセット間を連続補間し、手動操作・キャラ変更・ページ終了で解除する。

SAM 2.1の入力には透明画素の残存RGBを見せず、全身と人物上部30%を同じモデルで解析する。原画レイヤーはリサイズしない。幾何判定はアルファ128以上を使い、微小アルファによる全画面bbox化を防ぐ。候補マスクを `layers/candidates/` に保存し、失敗診断を再現可能にする。同時処理点数は `ai.sam2_points_per_batch`（既定8）で制御し、探索点総数・モデル・原画解像度は変更しない。

OBSサーバ・配信ページは利用者決定により廃止した。PicoAgent本体への組み込み、VRM化、macOS対応は現在の対象外である。以下に残る旧3D/OBS設計は現行機能を示さない。

**2026-09-04 利用者決定: 完成経路は3Dではなく2D/2.5Dを優先する。** 正面中心のVTuber用途では後ろ姿や自由な全身回転より、顔・髪・衣装の原画品質、まばたき、自然な口パクを優先する。既存の3D実装は失敗作として廃止し、新しい既定UIと生成経路へ持ち込まない。

新しい既定工程は次の3工程とする。各成果物をディスクへ確定してから次へ進み、途中再実行できるようにする。

```
[source/input.png]
   ↓ ① 背景除去
[source/isolated.png]
   ↓ ② 候補マスク生成（SAM 2.1 Hiera Tiny）＋決定的な意味付け
[layers/source.psd + layers/manifest.json + layers/parts/*.png]
   ↓ ③ 製品内2.5D自動リグ
[rig2d/rig.json + rig2d/parts/*.png]
```

実行時形式は本製品固有の `lvs-anime25d-v1` とし、Live2D/Cubism形式やPicoAgentの既存形式を正本にしない。PicoAgent統合時はPicoAgent側がこの形式へ対応する。正面から大きく外れる回転は対象外で、`MouthOpenY`、`MouthForm`、左右の目開閉、小角度の顔・体・腕・髪の動きを扱う。

2026-09-04に利用者が、権利表示のないSee-through必須重みを避け、別候補へ変更することを承認した。候補マスクにはコードと公式チェックポイントの双方がApache-2.0で公開されている `facebook/sam2.1-hiera-tiny` を使う。SAM 2は意味分類器ではないため、マスクの部位名、前後関係、必須パーツ判定をモデルへ推測させず、位置・包含・接続・入力シルエットとの関係にもとづく製品内の決定的処理で確定する。今回の制作・検証は非商用のローカル実験だが、公開リポジトリへ権利不明の重みは同梱しない。

2026-09-04にT10を実装した。既定UIと状態管理を `isolate → decompose → rig2d` へ切り替え、元キャンバスの透過PNG、10意味レイヤー、manifest、同内容の確認用PSD、`lvs-anime25d-v1`リグ骨格を工程ごとに保存する。SAM 2.1 Hiera Tinyはリビジョン `de431c4043854a71d8101e17995dfe596bf101a5` の4ファイルをSHA-256固定し、RTX 5060 Laptop GPUで1024×1536の実写テスト画像をCUDA実行した。顔・髪・左右の目・閉口/開口変形元・左右腕を個別に画像確認し、PSD 10レイヤーと同一キャンバスを再読込確認した。実際の開閉・揺れ・描画はT11で実装する。

以下の3D工程は採用理由ではなく、廃止理由を再検討しないための失敗記録である。

#### 4.2.1 旧3D試作の工程

```
[入力イラスト PNG/JPEG]
   ↓ ① 背景除去・キャラ切り出し        rembg (isnet-anime) — CPU可
[透過キャラ画像 1024x1024]
   ↓ ② 画像→3Dメッシュ生成             ローカル拡散モデル（GPU）
[テクスチャ付きメッシュ GLB / UVアトラス 2048^2]
   ↓ ③ リギング＋スキニング            テンプレート適合（4.3）
[リグ付き VRM / GLB]
   ↓ ④ 中立顔キャプチャ                 Rust CPU正射影ラスタライザ 1024^2
[neutral.png]
   ↓ ⑤ 表情・母音の画像生成             ComfyUI 顔インペイント（外部画像の持ち込みも可）
[<expr>.png, <expr>__{a,i,u,e,o,close}.png]
   ↓ ⑥ UVアトラスへ逆投影               フェイスパッチ投影（4.4）
[projected_<sig>_<expr>.png]
   ↓
[完成キャラクター]
```

**却下案: 三面図（character sheet）の生成。** 本家は Meshy の複数画像入力に合わせるため三面図を作っていたが、採用するローカルモデルは**単一画像から直接3D化できる**ため工程が不要になる。生成回数が1回減り、三面図間の不整合という失敗要因も消える。三面図が必要になるモデルへ将来差し替える場合のみ復活させる。

各工程の成果物は必ずディスクへ保存し、**途中から再開できる**ようにする。生成は数分かかるため、⑤だけやり直す・⑥だけやり直す、が日常操作になる。Tauri操作画面から全工程または選択工程だけを実行し、工程ごとの `pending / running / complete / failed` と理由を `character.json` へ原子的に保存する。女性Aでは中立キャプチャ失敗を保存後、同じキャラIDの④から再開して⑤・⑥まで完了できることを実機確認した。

2026-09-03の統合実走では、標準の女性A、長髪・装飾の女性C、大袖・短パンの女性Dを同じパイプラインへ通した。Aは任意の「ジト目」を加えた目6枚＋共通口形6枚から36アトラス、C/Dは目5枚＋共通口形6枚から各30アトラスを生成した。すべて `cargo xtask dev` で起動した実Tauri WebViewの960×640既定窓へ個別に読み込み、VRMと投影テクスチャの中央表示、一覧切替、5工程の完了表示を確認した。男性キャラクターは検証・同梱候補に含めていない。

### 4.3 旧3Dリギング — 非既定の試作記録

**採用案: テンプレートヒューマノイド適合 ＋ 自動スキニング。**

生成される素体は正面向きの人型に限られるため、汎用の骨推定は要らない。

1. メッシュのバウンディングボックスと左右対称軸から**全高・肩幅・頭部境界**を推定する
2. VRM 標準ヒューマノイドボーン（Hips / Spine / Chest / Neck / Head / 左右 Shoulder・UpperArm・LowerArm・Hand・UpperLeg・LowerLeg・Foot）を、その寸法へスケール配置する
3. **スキニングウェイトは heat diffusion（bone heat）系の自動ウェイトで算出**する。メッシュをボクセル化またはラプラシアン系で解く

| 観点 | この案 | 却下: UniRig 等の学習ベース自動リグ |
|---|---|---|
| 出力 | **VRM ヒューマノイド互換**（本家のFBXより資産価値が高い） | 独自骨。VRM化に別途マッピングが要る |
| VRAM | **不要** | 約8GB。3D生成と取り合いになる |
| 決定性 | **決定的。同じ入力で同じ結果** | 確率的。失敗したときに直せない |
| ライセンス | **クリーン** | 商用条項の確認が必要 |
| 弱点 | **A/Tポーズ以外の入力で破綻する** | 任意ポーズに強い |

弱点は工程②の時点で**A ポーズ相当に正規化して生成する**ことで潰す。それでも破綻する入力（座り・複雑ポーズ・非人型）は**検出して明示エラーにする**。黙って崩れたモデルを出さない。

**実装手段は自前の決定的なグラフ heat diffusion を採用する。** 三角形の辺から疎な頂点隣接行列を作り、各ボーン線分からの距離を熱源として18回拡散する。左右・上肢・下肢の領域制約を掛け、各頂点は上位4ボーンへ正規化する。追加の学習モデル・PyTorch・Blenderを必要としないため、同梱容量とVRAMを増やさない。Blender同梱はこの処理だけのために配布を数百MB増やすので却下した。

入力は正面のA/Tポーズに限定し、全高・幅・奥行き、左右端の手の高さと対称性で不適合を生成前に拒否する。Aポーズはウェイト算出後に肩を支点としてTポーズへ正規化し、VRM 1.0必須15ボーンを含む19ボーン、4ウェイト、埋め込みテクスチャ、3秒の `Idle` アニメーションを持つVRMとして出力する。VRMメタデータは利用者の権利を勝手に緩めない既定（作者のみ・再配布不可・改変不可）にする。

2026-09-03の女性3体実測では、A（標準服）15,788頂点、C（長髪・装飾）24,119頂点、D（ゆったりした袖）16,081頂点を各0.42〜0.48秒で処理し、全て19ボーン、最大4ウェイト、ウェイト和の最大誤差 `1.8e-7` 以下だった。Khronos glTF Validatorでエラー・警告0、公式VRM 1.0 JSON Schemaで適合を確認した。Three.js実表示で3体のTポーズと `Idle` の胸・頭の変形を前面と斜めから確認し、メッシュの崩壊はなかった。弱点は学習ベースの意味理解を持たないため、袖・髪・スカートなど体から離れた部位が近傍ボーンへ追従し、非人型・極端な体型・通常姿勢には対応しないこと。

### 4.4 旧3Dフェイスパッチ投影 — 非既定の試作記録

**ブレンドシェイプを使わず、顔を2D画像として編集してUVアトラスへ焼き戻す方式を独自実装する。**

AI生成メッシュにはブレンドシェイプが無く、UVも数千の微小アイランドに自動分割される。この制約下で表情を付ける現実解がこれである。

#### 4.4.1 キャプチャフレーム

頭ボーンとメッシュから顔の矩形を求め、**正射影で 1024x1024** をレンダリングする。

- 中心・前方向・上方向・矩形サイズ・前後の深度窓を `capture_frame.json` として保存する
- 頭部ウェイト頂点の矩形を1.42倍して投影対象範囲とし、正射影はさらに1.24倍の余白を取る。中心は頭部矩形から下へ0.55頭部高ずらし、目と口を固定インペイントマスク内へ揃える
- **中立時のスキニング済みメッシュをスナップショットとして保持**し、以降の投影は必ずそれを使う。ポーズが動いても投影がぶれないようにするため

実装済みの `facepatch` は glTF / GLB / VRM の skin、inverse bind matrix、ノードのワールド変換を読み、頭ボーンを含む中立時の頂点位置・法線・UV・頭ウェイトをスナップショット化する。中立画像は全三角形をCPUのZバッファでラスタライズし、頭ウェイトは⑥の投影対象絞り込みにだけ使う。キャプチャ時まで頭ウェイト100%へ限定すると顔面を落として髪だけになるため、この方式は採らない。

#### 4.4.2 逆投影

各表情画像について、UVアトラス（2048x2048）を次の手順で塗る。

1. 元アトラスを複製する
2. **投影対象の三角形を絞り込む**
   - 頂点のボーンウェイトが**頭ボーンに閾値以上**乗っている（体・髪の一部を除外）
   - 面法線がキャプチャ方向を向いている
   - 顔矩形の内側にある
3. **元画像側のマスクを作る** — α が閾値以下の画素、および中立画像との**差分が閾値未満の画素を除外**する。これで「実際に変わった部分」だけが書き込み対象になる
4. 採用三角形を **UV空間でラスタライズ**し、各テクセルについて
   - 対応する3D座標を求め、キャプチャカメラへ投影して元画像のUVを得る
   - **レイキャストで最前面かどうか判定する**（後頭部や髪の裏へ回り込むのを防ぐ。深度窓の内側の最前面ヒットだけ採用）
   - 元画像の画素を書き込む
5. **UV継ぎ目を数px膨張**させて隙間を埋める。非採用三角形のUV領域は避けて膨張させ、他パーツへ滲ませない
6. 書き込み画素が0なら**失敗として明示エラー**にし、診断画像を保存する

初期値は頭ウェイト 0.500、法線 0.350、深度窓 0.050、差分 0.075、α 0.030、継ぎ目膨張 4px、顔楕円の中心 `[0.500, 0.580]`・半径 `[0.240, 0.260]` とする。全値は設定から上書きできる。診断画像は採用三角形、元画像差分、UV書き込み領域の3枚を必ず出す。

2026-09-03 の単体検証では、女性3体（紺髪・薄紫髪・緑髪）の各36枚、計108枚を投影でき、書き込み0枚はなかった。書き込み画素は紺髪 1,516〜19,050（平均12,826.0）、薄紫髪 1,923〜18,037（平均13,982.9）、緑髪 5,005〜26,329（平均17,177.7）。three.js 0.185.1 のアンリット表示で、顔パーツが前面UVへ正しい上下方向で貼られ、3体間および表情・口形テクスチャを切り替えられることを確認した。意図的に顔楕円外へずらした差分は書き込まれないことをテストで固定する。

#### 4.4.3 キャッシュと署名

投影は重いので**生成時に一度だけ**行い、結果を保存する。実行時の表情切り替えは**テクスチャ差し替えのみ**。

キャッシュキーは以下を連結した**署名文字列**とする。値が1つでも変われば再計算する。

```
<パイプライン版>|<モデル識別>|<キャプチャ解像度>|<中心>|<前方>|<上>|<矩形>|<深度窓>|<各投影パラメータ>|<表情プロンプトのハッシュ>
```

本家と違い**表情プロンプト自体もハッシュして署名へ含める**。本家はプロンプトを変えてもキャッシュが効いてしまう構造だった。

#### 4.4.4 マテリアル

投影先は**アンリット（Unlit）マテリアル**とする。生成テクスチャには既に陰影が焼き込まれており、そこへライティングを重ねると二重に陰影が乗って破綻するため。本家も同じ判断をしている（`FlatUnlit`）。

### 4.5 リップシンク

本家と同じ**フォルマント最近傍**方式を独自実装する。機械学習を使わない。

- マイク 16kHz、解析窓 512サンプル、解析間隔 80ms、音量ゲート -40dBFS
- Hann窓 → **FFT**（本家は O(N²) の素朴なDFTだった。`rustfft` へ置き換える）
- 前後数ビンの移動平均でスペクトル包絡を作る
- F1 を 250〜1000Hz、F2 を「F1+150Hz」以降の 700〜3200Hz の最大ピークとして探す
- 各母音との距離を**対数周波数空間のユークリッド距離**で測り最近傍を採る。「あ」に軽くバイアスを掛ける
- 直近数フレームの多数決で平滑化し、**変化したときだけ**口形を通知する
- 無音が一定時間続いたら口を閉じる
- 目的の口形テクスチャが無ければ `__a` → `close` → 表情本体 の順にフォールバックする

母音フォルマントの初期値は日本語話者向けの一般値を用い、**設定で調整可能にする**。

**解析は Rust 側の1箇所だけで行い**、結果（現在の口形キー）を WebSocket で操作UIと OBS ページの両方へ配る。ブラウザ側で二重に解析しない。

同じ入力から VAD で発話区間を切り出し（終端無音・最短長・最大長は設定可能）、whisper.cpp へ渡して AITuber の会話入力にする。

2026-09-03 に `cpal` 0.18.2 と `rustfft` 6.4.1 で実装した。入力機器のネイティブ形式をモノラル16kHzへ変換し、FFT、スペクトル包絡、F1/F2最近傍、平滑化、VADをRust側だけで処理する。既知フォルマントの合成A/I/U/E/Oと無音、状態変化時だけの通知、VADの終端を自動テストで確認した。既定マイク48kHzから3秒間に16kHz解析窓を93個取得でき、入力ストリームを明示停止した。一度発生したバッファ過不足は利用者へ通知できるイベントとして上位へ渡す。

### 4.6 OBS 連携 — 本家を上回る設計

**先行方式の弱点**: MJPEG はアルファを持てないため、グリーンバック＋OBS側クロマキーが必須で、JPEG再圧縮の色滲みがクロマキー品質を落とす。

**採用案**: ローカル HTTP サーバが **three.js の描画ページ（`ui-stream/`）を配信し、OBS のブラウザソースがそれを直接描画する。**

```
Rust: 127.0.0.1:<port>
  GET  /           → ui-stream のページ（UIなし、body背景は透過）
  WS   /state      → 表情キー・口形キー・ポーズ・構図の状態を配信
  GET  /assets/... → モデル・テクスチャ
```

| 観点 | 本家 MJPEG | 本方式 |
|---|---|---|
| アルファ | **不可**。グリーンバック必須 | **可**。OBSブラウザソースはネイティブに透過対応 |
| 画質 | JPEG再圧縮あり | **再圧縮なし**。OBS側でGPU描画 |
| 転送量 | フレーム全体を毎フレーム | **状態のみ**（数百バイト/更新） |
| CPU | JPEGエンコードが常時走る | エンコード不要 |
| 解像度 | サーバ側で固定 | **OBS側のソース解像度に追従** |

トレードオフ: **モデルを本体プレビューと OBS で2回描画する**。対策として本体プレビューは低解像度・低フレームレートに落とし、OBS 側を主とする。実測で問題が出た場合の代替案（本体でオフスクリーン描画し WebP アルファ付きで転送）は却下案として保持するが、転送量とレイテンシで劣るため既定にしない。

ポートは本家同様の範囲を順に試して空きへバインドし、**全滅したら明示エラー**にする。有効状態は設定へ永続化して次回起動で復帰する。

実装は `axum` 0.8.9 のローカル専用サーバと `watch` 状態チャネルを使う。`ui-stream/` は `ui/shared/avatar-renderer.js` と同じvendored Three.jsを読み、WebSocketから受けた表情・口形・モデル・テクスチャ・姿勢・構図だけを反映する。モデル変更時はジオメトリ、テクスチャ変更時は旧テクスチャとマテリアル、ページ終了時はWebGLコンテキストまで解放する。実ブラウザでbody/htmlとも `rgba(0, 0, 0, 0)`、A→C→Dの状態切替、停止時の切断表示を確認済み。OBS本体への登録操作だけは利用者環境で行う。

### 4.7 ローカルモデル構成（VRAM 8GB 前提）

**すべて「このPC（RTX 5060 Laptop 8GB / RAM 32GB）で実際に回して検証してから採用する」。** 未検証のまま仕様へ固定しない。

2026-09-02 に T0 のライセンス監査を実施した。ここでいう「商用可」は、記載したライセンス本文の遵守を条件とする。著作権表示、ライセンス本文、NOTICE、変更表示、利用制限などの同梱義務は `THIRD_PARTY_NOTICES` と配布物で満たす。法令や入力素材に対する権利まで保証する意味ではない。

| 工程 | 採用候補・固定対象 | ライセンス | 商用利用 | 地域制限 | 重み・本体の再配布 | 生成物 | 採否・理由 |
|---|---|---|---|---|---|---|---|
| 背景除去 | `rembg` v2.0.83 + `skytnt/anime-seg` の `isnetis.onnx` | MIT + Apache-2.0 | 可 | なし | 可。Apache-2.0 の表示を保持 | 固有条件なし | **採用。** `rembg` が再ホストする重みではなく、作者公式の [モデル](https://huggingface.co/skytnt/anime-seg) を固定する |
| 背景除去の代替 | U-2-Net の上流重みを自前で ONNX 変換 | Apache-2.0 | 可 | なし | 可。LICENSE/NOTICEを保持 | 固有条件なし | 条件付き候補。`rembg` 配布の `u2net.onnx` は変換元を追跡できないため使わない |
| 2.5D候補マスク | [`facebook/sam2.1-hiera-tiny`](https://huggingface.co/facebook/sam2.1-hiera-tiny) | Apache-2.0 | 可 | なし | 可。LICENSE/NOTICEを保持 | 固有条件なし | **採用。** Meta公式はコード、学習済みチェックポイント、デモ、学習コードをApache-2.0と明記する。Hiera Tinyを固定。幾何的な意味分類の限界が実測され、意味解析の補助候補を比較中 |
| 意味解析比較A | [`microsoft/Florence-2-large-ft`](https://huggingface.co/microsoft/Florence-2-large-ft) | 重みMIT、実装ファイルApache-2.0表示も保持 | 可 | 明示的制限なし | 可。各権利表示を保持 | 固有条件の記載なし。原画の権利は別 | **実機比較のみ承認・製品未採用。** 3原画の顔/目/口/首を検出。共通選別後も女性Aの襟候補は欠落。公式旧実装のKVキャッシュを無効化して実行 |
| 意味領域解析 | [`IDEA-Research/grounding-dino-base`](https://huggingface.co/IDEA-Research/grounding-dino-base) | Apache-2.0 | 可 | 明示的制限なし | 可。LICENSE/NOTICEを保持 | 固有条件の記載なし。原画の権利は別 | **利用者承認により採用。** 固定版をsetup groundingで取得し、意味候補の幾何的選別後にSAM2で原寸分割する。3原画の首/口位置は改善するが人物全体の誤検出もあり、検出スコアだけで品質合格としない |
| 2.5D意味レイヤー分解コード | [`shitagaki-lab/see-through`](https://github.com/shitagaki-lab/see-through) `7f139bb25c46a0c8ac720d95ddab185fcda5451c` | Apache-2.0 | 可 | なし | 可。LICENSE/NOTICEと変更表示を保持 | 固有条件なし | **コードだけ適合。重み監査未完了のため採用保留。** 1280解像度BF16 Block SwapはRTX 5060 Laptopで完走済みだが、コードのライセンスを別配布の重みへ推定適用しない |
| See-through LayerDiff重み | [`layerdifforg/seethroughv0.0.2_layerdiff3d`](https://huggingface.co/layerdifforg/seethroughv0.0.2_layerdiff3d) `966721bb4ef2ddc3af3696862fa10b3f78d9785d` | Apache-2.0 | 可 | なし | 可。LICENSE/NOTICEを保持 | 固有条件なし | **重みまで適合。** 公式モデルカードのライセンス表示を確認 |
| See-through Marigold重み | [`layerdifforg/seethroughv0.0.1_marigold`](https://huggingface.co/layerdifforg/seethroughv0.0.1_marigold) `aa7a892f83ff68d7b09186a405ba08d5d33f770f` | **表示なし** | **未確認** | **未確認** | **未確認** | **未確認** | **採用保留。** 公式モデルリポジトリにモデルカードとLICENSEがなく、コード側Apache-2.0の適用対象だと確定できない |
| See-through SAM Body Parsing重み | [`24yearsold/l2d_sam_iter2`](https://huggingface.co/24yearsold/l2d_sam_iter2) `91c07b2150c0971b439dd050d34b6641f08679dd` | **表示なし** | **未確認** | **未確認** | **未確認** | **未確認** | **採用保留。** 公式モデルリポジトリは重みファイルだけで、ライセンス表示がない |
| 2.5D自動リグ | [`852wa/Anime2.5DRig`](https://github.com/852wa/Anime2.5DRig) `d48825867acd081de22b0e7b5585bb562288796d` | MIT | 可 | なし | 可。著作権表示とMIT本文を保持 | 固有条件なし | **ライセンス適合。** See-throughのPSDを27前後の描画パーツへ変換する実機POCは成立済み。前段重みの監査完了後に採用する |
| 画像→3D | `stabilityai/TripoSR` + `facebook/dino-vitb16` 設定 | MIT + Apache-2.0 | 可 | なし | コード・学習済み重み・設定とも可。各表示を保持 | 固有条件なし | **品質不適合。** ライセンスとVRAM 8GBには適合するが、女性3体すべてで顔・髪・衣装形状と正面テクスチャが崩壊した。256解像度化と正面原画投影でも改善せず、完成キャラクターには採用しない |
| 画像→3Dの代替 | `microsoft/TRELLIS-image-large` | MIT | 可 | なし | コード・学習済み重みとも可 | 固有条件なし | **現要件では不採用。** 公式要件はVRAM 16GB以上、Windowsは未検証扱いで、VRAM 8GB・Windowsネイティブ先行に適合しない。非商用の `diffoctreerast` を外す条件も残る |
| テクスチャ | TripoSR の出力 + 自前のマルチビュー投影 | MIT + 本プロジェクト | 可 | なし | 可 | 固有条件なし | **採用。** 地域制限のある Hunyuan3D-Paint は使わない |
| 表情・背景生成 | `cagliostrolab/animagine-xl-4.0` の `animagine-xl-4.0-opt.safetensors` | CreativeML Open RAIL++-M | 可 | 独自の除外国なし | 可。ライセンス・変更表示・利用制限を継承 | 出力はモデル派生物ではない。利用制限は適用 | **第一候補として採用。** [モデルカード](https://huggingface.co/cagliostrolab/animagine-xl-4.0) はSDXLと同一条項で追加制限なし |
| 構造拘束 | `diffusers/controlnet-canny-sdxl-1.0` | CreativeML Open RAIL++-M | 可 | 独自の除外国なし | 可。ライセンス・変更表示・利用制限を継承 | 出力はモデル派生物ではない。利用制限は適用 | **採用候補。** [モデルカード](https://huggingface.co/diffusers/controlnet-canny-sdxl-1.0)。depth/lineart/softedge は重みを特定して再監査するまで同梱禁止 |
| 同一性保持 | `h94/IP-Adapter` の SDXL Plus Face | Apache-2.0 | 可 | なし | 可。LICENSE/NOTICEを保持 | 固有条件なし | **採用候補。** アダプタと同梱image encoderを同じ[公式リポジトリ](https://huggingface.co/h94/IP-Adapter)から取得する |
| 指示ベース編集の代替 | `Qwen/Qwen-Image-Edit` | Apache-2.0 | 可 | なし | 可。LICENSE/NOTICEを保持 | 固有条件なし | ライセンス上は採用可。容量とVRAM 8GB適合はT2で判定する。[モデルカード](https://huggingface.co/Qwen/Qwen-Image-Edit) |
| 画像生成基盤 | ComfyUI v0.34.0 | GPL-3.0 | 可 | なし | 可。対応ソース、GPL本文、著作権表示を提供 | 対象外 | **採用。** 本アプリとはローカルHTTP越しの別プロセスとして分離する。[ライセンス](https://github.com/Comfy-Org/ComfyUI/blob/v0.34.0/LICENSE) |
| STT | whisper.cpp b4938 + `ggerganov/whisper.cpp` の `ggml-small.bin` | MIT | 可 | なし | 可。著作権表示・MIT本文を保持 | 固有条件なし | **採用。** [変換済み重み](https://huggingface.co/ggerganov/whisper.cpp)もMIT表記を確認 |
| 表情選択・会話LLM | llama.cpp v0.3.0 + `Qwen/Qwen2.5-1.5B-Instruct-GGUF` の Q4_K_M | MIT + Apache-2.0 | 可 | なし | 可。各LICENSE/NOTICEを保持 | 固有条件なし | **採用・実機検証済み。** [公式GGUF](https://huggingface.co/Qwen/Qwen2.5-1.5B-Instruct-GGUF)を使い、T7で日本語応答と表情JSON選択を確認した |

#### 4.7.0 ライセンス不適合で外した候補

| 候補 | 結論 | 外した理由 |
|---|---|---|
| Hunyuan3D-2mini / Hunyuan3D-Paint | **不採用** | [Tencent Hunyuan 3D 2.0 Community License](https://github.com/Tencent-Hunyuan/Hunyuan3D-2/blob/main/LICENSE) は EU・英国・韓国を許諾地域から除外し、出力の利用も地域外で禁止する。全世界向けデスクトップ製品と両立しない。100万MAU超の場合は別途許諾も必要 |
| Stable Fast 3D | **不採用** | [Stability AI Community License](https://huggingface.co/stabilityai/stable-fast-3d/blob/main/LICENSE.md) は商用利用者の登録、表示義務、全関連会社合算で年商100万米ドル到達時のライセンス終了と別契約を要求する。売上規模で製品が停止し得る条件を既定候補にしない |
| TencentARC/InstantMesh | **不採用** | リポジトリと公式重みの表示はApache-2.0だが、通常推論とテクスチャ出力が必須依存する `nvdiffrast` は第三者の商用利用を研究・評価目的に制限する。さらにマルチビュー生成元のZero123++公開重みはCC-BY-NC-4.0であり、商用製品へ同梱できない |
| FLUX.1 Kontext [dev] とそのGGUF量子化 | **不採用** | [FLUX.1 dev Non-Commercial License 1.1.1](https://huggingface.co/black-forest-labs/FLUX.1-Kontext-dev/blob/main/LICENSE.md) は重みの商用・本番利用を禁止する。量子化しても派生物として同じ制限を受ける。出力だけは商用利用可能だが、製品内推論は不可 |
| InstantID | **不採用** | コードはApache-2.0だが、[公式README](https://github.com/instantX-research/InstantID#disclaimer) が公開チェックポイントを研究目的限定、必須のInsightFace顔モデルを非商用研究限定と明記する |
| `rembg` が再ホストする `u2net.onnx` | **不採用** | `rembg` のMITはコードだけに適用され、配布者もONNXの変換元・変換者を追跡できないと[回答](https://github.com/danielgatis/rembg/issues/837#issuecomment-5172958667)している。上流重みから自前変換したものだけを代替候補にする |

旧3D候補については当時の監査停止条件には該当しなかったが、その後の実機品質検証で不採用になった。See-through方式はコードと一部重みだけが適合し、必須のMarigold重みとSAM Body Parsing重みに許諾表示がないため採用しない。新しい2.5D方式は、Apache-2.0がチェックポイントまで明記されたSAM 2.1と製品内の決定的処理で構成し、未ライセンス重みを必要としない。

ComfyUI v0.34.0 の初期ワークフローは**標準ノードだけ**で構成し、カスタムノードは現時点で1件も採用しない。追加が必要になった場合は、固定コミット、ライセンス、モデル重み、直接・推移Python依存を同じ基準で監査し終えるまで取得・同梱しない。ComfyUIのPython依存はPython 3.12/CUDA/PyTorchの組合せをT1/T2でロックし、配布前に全推移依存のライセンス一覧と対応ソースを生成する。

TRELLISの `diffoctreerast` は[研究・評価限定ライセンス](https://github.com/JeffreyXiang/diffoctreerast/blob/master/LICENSE)のため、候補として残すのはこの依存を完全に外したmesh生成経路だけとする。将来プレビューやoctree rendererのために黙って戻さない。

T5の実装はTripoSRの単画像推論へ、作者公式のanime-seg ONNXによる背景除去、A/Tポーズ入力検査、前景85%化、xatlas 0.0.11（MIT）によるUV展開、自前のCPU位置アトラス化、2048²テクスチャ生成を接続した。Python 3.12.13の共有環境でTransformersはComfyUIの下限を保った4.57.6へ固定し、5系とのDINO重みキー非互換を避ける。TripoSRとDINO設定はローカルパスだけを読み、未同梱ファイルを要求した場合は外部取得せず失敗する。ただし、これは技術検証経路として残すだけで、完成キャラクターの生成バックエンドには採用しない。

RTX 5060 Laptop GPU（表示VRAM 8.55 GB）で女性3体を実生成した。いずれも2048² UVアトラス付きGLBとしてThree.jsで読み込めたが、**「読み込める」ことを外観品質の合格と誤判定していた。** 2026-09-03の再検証では、背景除去までは3体とも正常、3D化直後から顔・髪・衣装が崩れ、リグ後とフェイスパッチ前の中立キャプチャでも破綻が残った。フェイスパッチで補える局所的な顔ぼけではない。

| 検証キャラ | 総時間 | ピークVRAM | 頂点 / 面 | UV書込画素 |
|---|---:|---:|---:|---:|
| 紺色ボブ・琥珀眼・星飾り | 13.27秒 | 2.03 GB | 15,788 / 20,732 | 2,025,095 |
| 薄紫ロング・紫眼・三日月飾り | 15.60秒 | 2.03 GB | 24,119 / 30,738 | 1,889,108 |
| 緑ミディアム・赤眼・葉飾り | 14.27秒 | 2.03 GB | 16,081 / 20,874 | 1,952,505 |

ピーク値はPyTorchの実割当1.893 GiBを十進換算した値。追加で紺色ボブをmarching cubes解像度192から256へ上げると、頂点15,788→28,183、面20,732→37,364、ピークVRAM約2.03→2.50 GB、生成15.7→19.8秒となったが、外観は改善しなかった。原画の正面色を768,666テクセルへ直接投影する試作も、形状と対応位置の崩れが支配的で改善しなかったため却下した。条件の悪い代替へ黙って降格せず、画像→3D工程は未解決として停止する。

#### 4.7.1 表情生成の品質リスク（最大の技術リスク）

比較対象のクラウド実装は、同一性・ポーズ・カメラ角度・左右の向き・髪型・色・ライティング・画風を維持し、表情だけを変える汎用画像編集モデルを使う。**ローカルモデルで同等を出すのは容易ではない**。

顔がわずかにずれる・色が変わる・左右反転するだけで、逆投影の結果が破綻する。緩和策:

1. **顔領域のインペイントに限定する**（画像全体を再生成しない）。マスク外は原理的に変わらない
2. **denoise を低く**（0.3〜0.45目安）して構造を保つ
3. **ControlNet（lineart / depth / softedge）で輪郭を拘束する**
4. **投影側の差分マスク（4.4.2 手順3）が最後の防波堤**になる。変わっていない画素は書き込まないので、多少のドリフトは吸収される
5. それでも破綻する場合は**中立との位置合わせ・色味合わせを投影前に行う**（本家も `align` / `colorMatch` を持っていた）
6. **ComfyUI を採用すれば、ControlNet・IP-Adapter・各種インペイントモデルがそのまま使えて緩和策の幅が広がる**（下記4.7.2）

T2の実測（2026-09-03）で、この工程は次の構成なら成立すると判断した。

- ComfyUI v0.34.0（`12d5279438bfefc058a269eae805ceab6047777f`）と Animagine XL 4.0 Optを使用する。Python 3.12.13、PyTorch 2.11.0+cu128で固定し、ワークフローは標準ノードだけで構成する
- T5依存追加後のPython 93ディストリビューションを再度ライセンス棚卸しし、非商用限定・地域制限付き依存が無いことを確認した。メタデータで不明だった `comfy-aimdo` 0.4.15と `comfyui_frontend_package` 1.49.6は固定版の公式LICENSEでGPL-3.0、T5で追加したOmegaConf 2.3.0はBSD-3-Clause、xatlas 0.0.11はMITと確認した。配布時はComfyUI本体と合わせてGPL本文、著作権表示、対応ソースの提供方法を含める
- 左右の目と口だけを別の楕円マスクでインペイントし、最後に中立画像へマスク合成する。マスク外を画素単位で不変にするため、狭いマスク内では denoise 0.65を採用する。閉眼の基準生成だけ0.85とする
- `blink` は閉眼の基準画像を1枚作り、その口だけを6口形へ変える二段階生成にする。閉眼を母音ごとに再生成した試行では、まつ毛が頬へ伸びる線状アーティファクトが出たため却下した
- ControlNet Canny（strength 0.35）は1枚55.28秒、差分画素率9.6207%で、輪郭保持の改善が見えず通常生成のおよそ2倍かかった。ワークフローから利用可能な状態は保つが、既定は無効とする

女性キャラクター3体で、各36枚を実生成して中立と並べて確認した。全体生成時間はモデル読込と閉眼基準の時間を除く合計で、差分画素率は中立画像に対する各出力の値である。

| 検証キャラ | 36枚生成時間 | 差分画素率 最小 / 最大 / 平均 | 目視結果 |
|---|---:|---:|---|
| 紺色ボブ・琥珀眼・星飾り | 707.17秒 | 5.0701% / 11.4099% / 10.3015% | 左右、髪、瞳、星飾り、衣装、顔位置を維持。6表情と6口形を識別可能 |
| 薄紫ロング・紫眼・三日月飾り | 634.83秒 | 5.1184% / 11.6328% / 10.4935% | 左右、髪、瞳、三日月飾り、衣装、顔位置を維持。6表情と6口形を識別可能 |
| 緑ミディアム・赤眼・葉飾り | 686.60秒 | 5.1342% / 11.6540% / 10.5401% | 左右、髪、瞳、葉飾り、衣装、顔位置を維持。6表情と6口形を識別可能 |

RTX 5060 Laptop GPU（VRAM 8.55 GB）での生成中ピーク実測は7.85 GB、1枚はモデル読込後およそ17〜20秒だった。初回起動は180秒を超えたため起動待ち上限を600秒とする。別に試した淡いピンク髪の柔らかい画風では、denoise 0.65と0.78で怒り・悲しみが十分に出ず、0.90では眼周辺が破綻したため合格3体から外した。したがって、**3体で成立したが画風を問わず同じ品質ではなく、先行製品の汎用性には届かない**。この弱点は4.10 Dの外部画像インポート導線でも補う。

**この工程は実装・実機検証済み。** T7でTauriの工程管理・設定・任意表情へ接続した。統合後の実生成はAの12レイヤー198.05秒、Cの11レイヤー180.32秒、Dの11レイヤー180.86秒で、差分画素率は5.00〜6.73%だった。

T6.5以降の現行出力は、目・眉5枚と共通口形6枚の**11レイヤー**である。上記36枚はT2時点の品質成立を確認した履歴値であり、実行時形式ではない。各表情の目・眉だけを `eyes/<exprKey>.png`、中立の口形だけを `mouth/<viseme>.png` へ生成し、逆投影直前に合成する。

ユーザー定義表情は `e_<プロンプトSHA-256先頭8桁>` を付け、同じプロンプトを重複登録しない。T7では「ジト目」を `e_5efde0e0` として実生成し、目周辺だけの編集と36組への逆投影を確認した。削除時は定義、目レイヤー、取込検査値、投影済みディレクトリを一緒に削除し、組み込み表情は削除させない。

背景は同じAnimagineとComfyUI標準ノードだけで、人物を除外した1344×756のネイティブ16:9画像を生成する。RTX 5060 Laptopで25.44秒、人物混入なしを実画像確認した。補間拡大は行わない。

#### 4.7.2 画像生成バックエンド: ComfyUI（決定）

**ComfyUI を同梱する**（2026-09-02 利用者決定。品質優先）。

| 方式 | 判断 | 理由 |
|---|---|---|
| **ComfyUI 同梱** | **採用** | ControlNet・IP-Adapter・インペイント・最新モデルがそのまま使える。**4.7.1 の緩和策を全部打てるのはこちらだけ**で、最大の技術リスクを下げられる |
| `stable-diffusion.cpp`（GGUF） | 却下 | 配布が軽くPython不要という利点はあるが、対応モデル・拡張が限られ、4.7.1 の緩和策を全部は打てない。**品質が製品の成否を決める工程で軽さを優先しない** |

**受け入れた代償**（[AGENTS.md](AGENTS.md) の「犠牲にする場合は伝える」に基づき明記する）:

- 配布サイズが増える（ComfyUI 本体＋カスタムノード＋Python 依存）
- **Python が画像経路にも入る**（3.1）。起動時間とプロセス管理が複雑になる
- ワークフロー JSON とカスタムノードの版を保守し続ける必要がある
- ComfyUI 常駐分の VRAM を、画像→3D生成と取り合う（3.1 の規則で対処）

運用上の規則は 3.1「ComfyUI 固有の規則」を参照。

### 4.8 動画オーバーレイ合成

先行製品は「自由な動作」を骨アニメーションではなく、**透過（クロマキー）動画を3D画面へ重ねる**ことで実現していた。生成3Dモデルの自動リグはフルボディモーションに耐えないため、動きの表現を動画へ逃がす割り切りである。

**この合成レイヤーは動画生成AIと独立している。** 動画生成そのものは VRAM 8GB では実用にならないため初期スコープ外だが、**合成機能だけは実装する**。素材は利用者が持ち込めばよく、実装は安価。

- 動画は three.js のシーンへ板ポリゴンまたはHTMLレイヤーとして重ねる
- **アルファ付き動画（WebM/VP9 alpha）を第一とし、クロマキー抜きは代替とする。** ブラウザで描画するためαを直接扱えるので、先行製品のようにクロマキーを強制されない
- 再生条件（用途・長さ・リップシンクを止めるか）はキャラクター定義に持つ（5.1）
- 再生する動画の選択は 4.9 に従う

### 4.9 表情と動画の自動選択

**生成タスクではなく選択タスクとして設計する。** 発話テキストを入力に、「どの表情キーを出すか」「どの動画を再生するか」を**候補一覧から選ばせて JSON で返させる**。

- 候補（表情キー・動画ID）はこちらが列挙して渡す。モデルに自由記述させない
- 出力は固定スキーマの JSON。**スキーマ外の応答は不採用にして既定へ落とす**（`// フォールバック許可: 理由` を付ける）
- この用途なら **1.5B 級の小型モデルで足りる**。大きなモデルを積んで VRAM を画像・3D生成と取り合わない
- 選択結果は 4.6 の WebSocket で操作UIと OBS ページへ配る

T7では llama.cpp b10621（v0.3.0系）とQwen2.5 1.5B Q4_K_M、whisper.cpp b4938とWhisper smallをSHA-256固定で取得する。会話は候補キーだけを列挙した固定JSONを `--single-turn --simple-io` で1回生成し、候補外キーは採用しない。入力は2,000文字、人格は4,000文字を上限とする。音声は日本語16kHz WAVをwhisper.cppへ渡し、文字起こし→会話→表情切替を1操作で逐次実行する。画像生成・3D生成と同じ実行ロックを使い、VRAMを同時使用しない。

RTX 5060 Laptopで、女性音声「こんにちは、今日はいい天気ですね」を「こんにちは、今日は良い天気ですね。」と認識し、続けて `{"reply":"こんにちは、良い天気ですね！","expressionKey":"smile"}` を返した。ウォーム状態の個別実測はSTT 2.46秒、会話6.53秒、連結経路は16.1秒だった。クラウド通信・APIキーは使わない。

## 4.10 先行製品を上回る点

### 現行2.5Dの比較境界

| 項目 | ひよりを目標とした現在の状態 |
|---|---|
| 原画保持 | 中立PNGを別保存し、描画時の重複アルファ合成を排除。補間拡大素材は使わない |
| 口・まばたき | 原画由来の上下唇で実写の色・艶を保持。上まつげを原画から測定し半閉眼をクリップする方式へ変更、女性Aの二重表示は軽減した。むぎの輪郭段差・目尻の残留線を確認し調整中。白目/虹彩/下まぶた、口内・舌、連続動作の品質は未合格 |
| 小角度移動 | 実ブラウザで首肩の裂け目を修正確認。連続した平面変形であり、立体的な顔回転や隠れた部位の復元では劣る |
| 別原画への再現性 | 未達。Grounding DINO+SAM2の通常経路で女性A/むぎ/実写を再生成。むぎの髪マスクが目口を100%覆った誤分割は、検出済み目口の除外点により左目3.8%/右目4.9%/口0%へ減少した。これは正解率ではない。閉眼補完を実マスク内に制限し髪を保護するが、髪際・唇・内部素材分割は未合格。比較時の同入力2回の一致を品質合格へ昇格しない |
| 描画素材の展開量 | 原寸切詰めと口原画を含む読込5画像のRGBA計算量は女性A6.31 MB、むぎ26.35 MB、実写3.89 MB。画素の縮小なし。全体Canvasの合成・テクスチャ更新が別にあるため、ブラウザ全体のRAM/VRAMやひよりとの消費量比較は未実測 |
| OBS・VRM | 現行スコープ外。下表の旧方式の利点を現行製品の利点として扱わない |

模倣が目的ではない。**先行製品の構造的な弱点を潰した上位版を作る。** 比較値は本節へ集約し、新しい実測が出たら更新する。

### 既に設計へ織り込んだもの

| 項目 | 先行製品 | 本実装 |
|---|---|---|
| OBS出力 | MJPEG。αを持てずグリーンバック必須、JPEG再圧縮で色が滲む | **真のα出力**。クロマキー不要、再圧縮なし、解像度はOBS側に追従（4.6） |
| 出力形式 | FBX（独自リグ） | **VRM**。VRChat・VSeeFace など他ツールでも使える（4.3） |
| 動作コスト | 生成ごとに課金（キャラ800pt、表情セット200pt） | **無料・オフライン・回数無制限** |
| 動画オーバーレイ | H.264＋クロマキー | **アルファ付き動画（WebM/VP9）を第一**（4.8） |
| リップシンク | O(N²) の素朴なDFT | **FFT** |
| キャッシュ署名 | プロンプトを含まない。プロンプトを変えても古い結果が残る | **プロンプトのハッシュを署名へ含める**（4.4.3） |
| 構図データ | 配列。同じ `presetId` が重複し値も同一 | **キー引き**（5.1） |
| 識別子 | 日本語をファイル名に使用 | **ASCII キーと表示名を分離**（5.1） |

### 課題の性質を取り違えない — VRAMで解ける課題と解けない課題

**「課題は概ねVRAMだけ」という理解は誤り。** ここを混同すると、GPUを増強すれば解決すると期待して外し、対処の判断を誤る。

#### VRAMで解ける課題（工学的に必ず解ける）

| 課題 | 対処 |
|---|---|
| モデルがVRAMに載らない | 逐次オフロード、量子化、タイル分割、ComfyUIと3D生成を同時に走らせない（3.1） |
| 生成が遅い | オフロードを減らす。GPU増強が素直に効く |

**遅くなるが動く**種類の問題。時間と工夫で必ず解決する。

#### VRAMでは解けない課題（本命のリスク）

| 課題 | なぜVRAMで解けないか |
|---|---|
| **表情の同一性保持**（4.7.1） | **モデルの素性の問題**。VRAM無制限でも、オープンモデルが `gpt-image-2` の「向き・髪型・画風を保ったまま表情だけ変える」能力に届く保証がない。GPUを積んでも改善しない |
| リギング品質（4.3） | テンプレート適合と学習ベースのアルゴリズム差 |
| 3Dメッシュ品質 | Tripo は商用専用モデル。学習データと規模が違う |
| **ライセンス** | 商用不可・地域制限なら、性能に関係なく使えない（T0） |

**外部サービスの検討が必要になるとしたら、VRAM不足ではなく表情生成の品質が理由になる可能性が高い。**

この切り分けは T2 の実測で確定する。**「VRAMが足りなかったから」と「品質が届かなかったから」を報告時に混ぜない。**

#### 4.7.3 表情品質が届かないときの手順（外部サービスは最後）

**Stable Diffusion 系のローカル手段を使い切る前に外部サービスを提案しない。** ComfyUI を採用したのは、この段階を全部踏めるようにするため。上から順に試し、**どこまで試して何が足りなかったかを記録**してから次へ進む。

| 段階 | 手段 | 備考 |
|---|---|---|
| 1 | **ワークフロー調整** — denoise を下げる、マスク精度を上げる、ステップ数・サンプラー・CFGを詰める | まずここ。設定だけで済むことが多い |
| 2 | **構造拘束を足す** — ControlNet（lineart / canny / softedge / depth）で輪郭と構図を固定する | 左右反転と位置ずれに直接効く |
| 3 | **同一性保持の専用手段** — ライセンス適合済みの IP-Adapter（FaceID系）を入れる | 素性の差をかなり埋められる。InstantIDは4.7の監査で不採用 |
| 4 | **モデルを替える** — アニメ特化のSDXL finetune、**Qwen-Image-Edit などライセンス適合済みの指示ベース編集モデル**を試す | 指示ベース編集モデルは `gpt-image-2` に最も近い性格のオープンモデル。**優先的に評価する**。FLUX.1 Kontext [dev]は不採用 |
| 5 | **表情LoRAを使う／自作する** — 既存の表情LoRA、または対象キャラで学習する | キャラ固定なら学習が最も効く。VRAM 8GB でも LoRA 学習は可能 |
| 6 | **アルゴリズム側で吸収** — 差分マスクを厳しくする、投影前の位置合わせ・色味合わせを強化する（4.4.2） | 生成の粗を投影側で殺す。**最後の防波堤** |
| 7 | **外部生成画像のインポート導線**（4.10 D） | **製品はクラウドを呼ばないまま品質を得られる。** 利用者がブラウザの Gemini 等で作って持ち込む。**サブスク範囲内なら追加費用ゼロ**。段階9より先に検討する |
| 8 | **仕様を変える** — 表情の種類を減らす、母音を減らす | 品質を保ったまま範囲を狭める |
| 9 | **外部サービスをアプリから呼ぶ** | ここまで全部試して届かなかった場合のみ。**前提が変わるので利用者の判断が要る** |

段階7と段階9は性質がまったく違う。**段階7は製品の前提を1つも緩めない**（呼ぶかどうかは利用者が製品の外で決める）。段階9は製品がクラウドに依存する。**この2つを混同しない。**

段階9へ進む場合は [AGENTS.md](AGENTS.md)「ローカルで成立しないと判明した場合」に従い、**1〜7で何を試して何が足りなかったかを実測値で示した上で**提案する。採用が決まっても、**表情生成の工程だけをクラウドへ出し、他はローカルのまま保つ**。全体をクラウド前提へ作り替えない。

##### 段階9の具体案: 「生成時だけクラウド／配信は完全ローカル」

段階7（インポート導線）でも足りず、アプリから外部サービスを呼ぶことになった場合、**代償は見た目より小さい**。この構造を理解した上で判断する。

表情生成は**キャラクター作成時に1回だけ**走る。以降の配信・リップシンク・表情切替は投影済みテクスチャの差し替えだけで、**完全にローカル**（4.4.3）。つまりクラウドに出るのは初回セットアップの N 枚だけで、**VTuberとして実際に使っている間はオフライン・プライバシー保持のまま**。

| 性質 | 全ローカル | 生成時のみクラウド | 全体クラウド（先行製品） |
|---|---|---|---|
| 配信中のオフライン動作 | ○ | **○** | ○（配信部分はローカル） |
| 配信中のプライバシー | ○ | **○** | ○ |
| キャラ作成のオフライン動作 | ○ | ✕ | ✕ |
| 生成ごとの費用 | 無料 | **N枚分のみ** | キャラ800pt＋表情200pt等 |
| 利用者の手続き | 不要 | APIキー登録が必要 | アカウント登録・課金 |
| 表情の同一性保持 | 未知（T2で判明） | **高い** | 高い |

**候補**: Nano Banana 系（Gemini の画像編集モデル）、`gpt-image` 系。いずれも「同一性を保ったまま指示通りに編集する」ことに強く、先行製品が採用したのと同じクラスの道具。

**採用前に必ず確認する**（本ファイル執筆時点では未検証）:

- 現行の正確なモデル名・版・**画像1枚あたりの費用**
- **利用規約**。生成物の商用利用可否、入力画像の学習利用可否（**利用者のイラストを送ることになるため重要**）
- APIキーを利用者に持たせる形にするか、こちらが持つか（後者なら費用負担の設計が必要）
- オフライン専用として売れなくなることを、製品説明でどう扱うか

**この判断は利用者が行う**（[AGENTS.md](AGENTS.md)）。エージェントは段階1〜7の実測値と、この表の埋まった版を揃えて提案するところまで。

### 先行製品を下回る点（隠さず記録する）

クラウドをローカルへ置き換える以上、**必ず劣る部分が出る**。ここを曖昧にしたまま進めると、完成間際に「思っていたものと違う」になる。判明した時点で更新する。

| 項目 | 先行製品 | 本実装 | 深刻度 |
|---|---|---|---|
| **動作要件** | CPUのみで動く（GPU用バイナリを一切同梱していない） | **CUDA対応NVIDIA GPU必須、VRAM 8GB以上。決定事項**（2026-09-02 利用者判断） | **高。明確な後退。** GPU非搭載機・AMD/Intel GPU機では動かない。**利用者了承済み** |
| **表情画像の品質** | `gpt-image-2`。同一性・向き・画風の保持が非常に強い | 女性3体では成立。ただし柔らかい画風の1体は表情が弱く不採用。**画風を問わない汎用性は劣る**（4.7.1） | **中〜高。基本経路は成立したが、入力画風による不合格が残る** |
| **配布サイズ・起動時間** | 約1.5GB、Unity単体 | 開発配置のComfyUI 12.00 GB、Python 5.89 GB、モデル3.46 GB、推論エンジン2.34 GBで合計**23.70 GB**（十進）。配布時の圧縮後容量は未実測 | 中〜高。**品質優先で受け入れ済み** |
| **3Dメッシュ品質** | Tripo の商用モデル | TripoSR。女性3体すべてで顔・髪・衣装とテクスチャが崩れ、256解像度化・正面原画投影・後段フェイスパッチでも救済不能 | **高。現方式では正面配信も成立しないため、完成バックエンド未決定** |
| **リギング品質** | Meshy の学習ベース自動リグ | 女性3体のTポーズとアイドル変形は成立。テンプレート適合のため、**非人型・極端な体型、独立した長髪・装飾・大きな衣服の物理的な揺れは劣る**（4.3） | 中 |
| **生成時間** | クラウドの強力なGPUで並列。1画像約50秒 | RTX 5060 Laptopで直交化した11枚を実生成し、合計193.59〜198.78秒。現実装は逐次生成 | 低〜中。従来36枚の634.83〜707.17秒から約69〜73%短縮 |
| **動画生成** | あり（BytePlus） | **なし**（4.8。合成レイヤーのみ実装） | 中 |
| **初期ダウンロード容量** | 約1.5GB | 開発配置で合計23.70 GB。初回取得は機能利用意思とCUDA GPU確認後だけ行う | 中〜高 |
| **対応OS** | Windows / macOS | **Windows先行**。macOSは後続 | 中 |
| **完成度・安定性** | 出荷済み製品 | 新規実装 | — |

**上回る点とのトレードオフ**: 動作要件と生成品質を犠牲にして、オフライン動作・無料・無制限・プライバシー・VRM出力・真のα配信を得る構図になる。この交換が受け入れられるかは用途次第であり、**受け入れられない領域があることを認めた上で設計する**。

実測して数値が確定したら、見込みを実測値へ置き換える。**楽観的な見込みのまま放置しない。**

### さらに上を狙う案（方針は承認済み）

**利用者の判断: 本プロジェクトの制約内（クラウド不使用・VRAM 8GB）で成立するなら、これらを実装してよい。** ただし制約と衝突した場合は本体機能を優先し、成立しなかった案と理由を記録して見送る。無理に押し込んで基本動作を不安定にしない。

**A. 表情と母音を直交させ、生成枚数を減らす。**

先行製品は**表情ごとに母音6枚を丸ごと生成**している。表情 N 種で **7N 枚**（本体＋母音6）必要になり、生成時間・費用・ディスクがすべて N に比例して膨らむ。実測で1画像あたり約50秒なので、表情を1つ増やすたびに数分かかる。

投影側には既に**領域マスク**（顔マスク楕円・特徴部マスク）がある。これを使って

- **目・眉の差分**＝表情（N枚）
- **口の差分**＝母音（6枚）

を別レイヤーとして生成し、**投影時に合成**すれば **N+6 枚**で済む。表情10種なら 70枚 → 16枚。

リスク: 目と口が独立に動くと不自然な組み合わせが出る（例「悲しい目＋大きく開いた口」）。緩和として、表情ごとに口の開き量を制限する係数を持つ。**T3 完了後に、投影の領域分離が実際に破綻しないかを見てから判断する。**

**B. 表情遷移の補間。** 先行製品はテクスチャの離散切り替えで、表情が瞬間的に変わる。投影済みテクスチャ間を**クロスフェード**すれば滑らかになる。実装コストは低い。

**C. 自動まばたき・アイドル揺れ。** `blink` テクスチャは既にあるので、ランダム間隔で差し込むだけで生っぽさが出る。頭・体の微小な揺れも手続き的に足せる。

**D. 外部生成画像のインポート導線（一級機能として実装する）。**

投影前の1024px画像を**外部で作って持ち込める**ようにする。当初は「気に入らないとき手直しする」程度の位置づけだったが、**制約を緩めずに品質問題を解決する主要な出口**なので優先度を上げる。

仕組みは単純で、パイプラインの工程⑤（表情生成）の出力先へ、利用者が用意した画像を置けるようにするだけ。工程⑥（逆投影）以降はそのまま動く。

これが効く理由:

- **製品はクラウドAPIを一切呼ばない。** 呼ぶかどうかは利用者が製品の外で決める。前提を1つも緩めない
- 利用者は好きな手段を選べる — ブラウザの Gemini（Nano Banana 系）、`gpt-image`、手描き、既存のイラスト。**利用者側でブラウザ操作により一貫性のある生成ができた実績がある**
- ローカル生成が段階1〜7（4.7.3）で届かなかった場合の、**外部API採用（段階9）より優先される回避策**になる
- オフライン・無料で使いたい利用者は、ローカル生成のまま使い続けられる

実装で必要なこと:

- 受け入れる画像の要件を明示する（1024px正方形、中立キャプチャと同じ画角・向き、透過または白背景）
- **投入された画像を中立と照合し、位置ずれ・左右反転・色差を検出して警告する。** 外部生成物ほどここが狂いやすい。黙って投影して破綻させない
- 差し込んだ画像はキャッシュ署名の対象に含める（差し替えたのに古い投影が残らないように）
- 中立キャプチャを**書き出せる**ようにする。これが無いと外部で作りようがない

**製品にブラウザ自動操作を組み込むことはしない。** 各利用者にサブスクとログインを要求することになり、UI変更で壊れ、Web UI の自動操作は一般に利用規約上グレー。**開発作業と利用者自身の手作業では有効だが、製品の実行経路には入れない**（[AGENTS.md](AGENTS.md)）。

#### 4.10.1 T6.5 実装結果

- **Aは採用・実装済み。** 現行の5表情では30組を11回の画像生成（5+6）から復元する。悲しみ0.55、怒り0.72、閉眼0.65を口差分の係数とし、その他は1.0。女性3体で11枚ずつを新規生成し、合計90組を合成・逆投影して書き込み0がないことを確認した。生成合計はA 193.59秒、C 198.78秒、D 194.92秒で、従来36枚の634.83〜707.17秒から約69〜73%減った。書き込み画素はA 11,550〜16,890、C 14,694〜21,617、D 14,919〜22,864。
- **Bは採用・実装済み。** OBSと操作プレビューの共通Three.js描画で2枚のテクスチャをGPU上で補間する。既定160ms、0〜5000msで設定可能。頻繁な口形切替で再読込・競合しないようURL単位でキャッシュし、最新状態だけを適用する。
- **Cは採用・実装済み。** 同じ口形の閉眼テクスチャを2.8〜6.5秒のランダム間隔で140ms差し込み、モデルへ周期4.2秒・振幅0.7度の微小揺れを加える。全値は `avatar.*` 設定から変更できる。OBS実ブラウザの連番で開眼→中間→閉眼、表情クロスフェード、揺れを確認した。
- **Dは採用・実装済み。** `expression-import` は中立キャプチャを同じ出力へ書き出し、1024px RGBA、ASCIIキー、FFT相互相関による位置ずれ・左右反転、平均色差を検査する。投入画像と中立画像の画素および検査設定をSHA-256署名へ含める。女性Aの外部相当画像はずれ0px、通常相関0.996323、反転相関0.933104、色差0.004272で警告なしだった。ブラウザやクラウドAPIを製品から呼ぶ実装は入れていない。

## 5. データ形式

### 5.1 キャラクター保存形式

1キャラ = 1フォルダ。本家の構造を参考にしつつ、**日本語をファイル名に使わない**・**キーと表示名を分離する**点を変える。

```
characters/<characterId>/
  character.json           キャラ定義（下記）
  manifest.tsv             ファイル一覧＋サイズ＋ハッシュ（整合性検証）
  source/
    input.png              入力イラスト（原本。上書き禁止）
    isolated.png           背景除去後
  model/
    mesh.glb               生成メッシュ（リグなし）
    rigged.vrm             リグ付き。実行時に使うのはこれ
    thumbnail.png
  facepatch/
    capture_frame.json     キャプチャフレーム定義
    signature.txt          パイプライン署名（キャッシュ判定）
    neutral.png            中立キャプチャ 1024^2
    expr/eyes/<exprKey>.png              目・眉レイヤー 1024^2
    expr/mouth/<viseme>.png              共通口形レイヤー 1024^2
    expr/<exprKey>.import.json           外部投入時の検査値・署名
    projected/<exprKey>/<viseme>.png     合成・投影済みアトラス 2048^2
  backgrounds/<bgId>.png
```

`character.json`:

```jsonc
{
  "schemaVersion": 1,
  "characterId": "c_01J...",         // ASCII のみ
  "displayName": "お試しキャラ",       // 表示名は日本語可。ファイル名には使わない
  "createdAtIso": "...",
  "updatedAtIso": "...",
  "personaPrompt": "",                // AITuber人格設定
  "model": { "mesh": "model/mesh.glb", "rigged": "model/rigged.vrm", "thumbnail": "..." },
  "expressions": [
    { "key": "smile", "label": "笑顔", "prompt": "...", "isBuiltIn": true },
    { "key": "e_7f3a91c2", "label": "ジト目", "prompt": "...", "isBuiltIn": false }
  ],
  "framings": {                        // 配列ではなくキー引き（本家の重複バグを踏まない）
    "green_screen": { "yaw": 0.0, "pitch": -15.4, "scale": 1.0, "offsetX": 0.0, "offsetY": 0.0,
                      "armPose": { "leftUpperArm": [0,0,0], "leftLowerArm": [0,0,0],
                                   "rightUpperArm": [0,0,0], "rightLowerArm": [0,0,0], "head": [0,0,0] } }
  }
}
```

- **`characterId` と `exprKey` は ASCII のみ**。表示名・プロンプトは別フィールドに持つ
- ユーザー定義表情のキーは `e_<プロンプトのハッシュ8桁>`。同じプロンプトなら同じキーになり再生成を避けられる
- 入力イラスト原本 `source/input.png` は**上書き・削除しない**

### 5.2 状態ファイルの扱い

設定・キャラ一覧・生成履歴など「全体を読んで全体を書き戻す」ファイルは、**必ず原子的に書く**（一時ファイル → `sync_all` → `rename`）。読めなかったファイルは退避してから空で継続し、**空の状態をそのまま保存し返さない**（[AGENTS.md](AGENTS.md)）。

## 6. 設定

設定の正本は `src-tauri/src/config.rs` と永続ファイル `app_config_dir/config.json`。優先順位は **永続ファイル > 環境変数 > ハードコード既定値**。`ai` の部分指定は指定キーだけを上書きし、省略したキーの環境変数を保持する。全キーの一覧は [docs/SETTINGS.md](docs/SETTINGS.md) が正本。

本実装は**クラウドAPIを使わないため、APIキー設定を持たない**。将来クラウドを併用する変更が入る場合のみ、秘密値の保護規則（OS資格情報ストアへ預ける・平文で `config.json` に置かない）を [AGENTS.md](AGENTS.md) に従って追加する。

## 7. 検証方針

| 対象 | 手段 |
|---|---|
| クラウド非依存 | **生成中に外向き通信が発生しないことを機械検査する。** モデルDL以外のHTTPクライアント生成を検出するテストを置く |
| フェイスパッチ投影 | 既知のメッシュ＋既知の編集画像を入力し、**投影画素数と書き込み領域**をゴールデン値と比較する |
| リップシンク | 合成した母音音声（既知のフォルマント）を流し、判定結果が期待通りか検証する |
| リギング | 生成モデルの標準ヒューマノイドボーンが**全て存在し、想定範囲の位置にある**ことを検証する |
| OBS出力 | ページが透過で描画され、WebSocketの状態更新が反映されることを実ブラウザで確認する |
| 画質・見た目 | 自動化しづらいため、**最終目視は利用者に委ねてよい**（[AGENTS.md](AGENTS.md)）。ただし前段の数値検証は必ず置く |

検証コストの順序は `cargo check` → `cargo test --lib` → 最後に GUI 起動。GUI 起動と生成の実走は高コストなので、複数の確認をまとめて1回で行う。

## 8. 対応環境

- **Windows 10/11 x64** を先行。macOS / Linux は後続（[docs/TASKS.md](docs/TASKS.md)）
- GPU: **CUDA対応 NVIDIA GPU、VRAM 8GB 以上を必須とする**（2026-09-02 利用者決定）
  - **CPUフォールバックを実装しない。** 何十分も待たせる経路を用意するより、動かないことを明示する方が誠実
  - 起動時に GPU を検査し、要件を満たさない場合は**何が足りないかを具体的に表示して終了する**。中途半端に起動して生成時に落とさない
  - AMD / Intel GPU は非対応。将来 ROCm / DirectML を検討する場合は別途判断する
- ディスク: ComfyUI とモデル一式で相当量を要する（実測後に確定する。当初見込み15GBは ComfyUI 採用により増える）

## 9. 未確定事項

実装へ入る前、または実装中に決める必要があるもの。結論が出たらこのファイルへ書き、[docs/TASKS.md](docs/TASKS.md) から消す。

1. 表情生成の ComfyUI ワークフロー構成と実測品質。4.7.3 の段階1〜7のどこで足りるか（T2）
2. 自動スキニング（heat diffusion）の実装手段。Blender 同梱の要否（T6）
3. 会話・表情選択LLMの品質確定（ライセンス適合候補は4.7で選定済み。1.5B級で足りる見込み）
4. 動画生成機能を将来実装するか

**決定済み**（2026-09-02 利用者判断）: 製品名 `LocalVTuberStudio`、画像バックエンドは ComfyUI 同梱（品質優先）、動作要件は CUDA 必須。
