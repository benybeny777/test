# TASKS.md — 未完了作業と着手順

実写の依頼対象はPicoAgentの「実写・物理演算テスト」（`photoreal-prototype`）。別の女性`c_828ead7c98ab`と派生候補`c_7f8182efe7c0`は対象外と訂正されたため、確認画面から除外し、素材は削除せず保留する。その比較結果を依頼対象の成果・品質合格に数えない。正しい原画のSHAを照合し、同じ通常工程で`c_386f432bc3dd`を生成。口内・歯を含む実写の口表現を検証中で、大きな開口時の鼻下・顎の巻き込みと平坦な歯の表現は未合格。半透明の内部分割境界の描画修正は、むぎ・女性Aでも検証を続ける。製品モデル採用とは分ける。

次の検証: 「むぎ・耳＋閉眼修正候補」`c_df28cf7d4d11`の耳と閉眼の見た目は利用者承認済み。この候補を保持し、口の連続動作と別原画での再現性を検証する。モデルの製品採用や全キャラの品質合格とは分ける。

女性Aの閉眼比較は固定1024px・原寸頭部のみの両方式で構図変更を起こしたため不採用。原寸頭部と目の局所編集を組み合わせた生成は895.59秒で完了し、口・首のRGB差は0だった。比較版5の`c_df85ec1d7960`を確認画面へ追加し、髪の所有マスクと編集範囲で肌下地のはみ出しを制限した。半閉眼・完全閉眼はChromeで確認済みだが、別原画全般の再現性・素材充足・最終品質は未合格。Qwenの通常パイプライン採用は利用者判断待ち。

動作比較の判断待ち: 利用者が可動モデル側の耳輪郭の描き直しを承認したため、`/ui/check.html`へ「むぎ・耳輪郭修正候補」（比較仕様版19、`c_2379190bb3b3`）を追加した。生成した耳と原画の耳を既存DINO/SAMで別々に抽出し、新しい耳を合成して古い耳を全素材から除く。大きな矩形残片・元の耳の二重表示は解消を確認したが、耳が原画より露出する形で進めることは利用者承認済み。髪との接合部・色差、口/閉眼の造形、別原画での再現性は未合格。原画ファイル・既存3体は維持。補完モデルの製品採用は未決定。

実装を担当する作業エージェント（Codex を想定）向けの指示書。着手前に必ず [AGENTS.md](../AGENTS.md)（恒久ルール）→ [SPEC.md](../SPEC.md)（現行仕様）の順で読む。

各タスクは**1タスク = 1 PR**とする。完了したらこのファイルの該当項目を消し、結論を `SPEC.md` へ移す。

## 実装順タスク

### T11. `lvs-anime25d-v1`リグと共通レンダラー

- **比較実行を利用者承認済み:** SPEC 4.7のQwen-Image-Layered / Qwen-Image-Edit-2511をComfyUI標準ノード・非量子化重みで比較する。重み5ファイルは取得・SHA検査済み。むぎ原寸1024px/50stepでLayered約74.7分、Image-Edit約21.7分で完走。前者は混合・重複レイヤー、後者は肌色・口・まつげの変更で、生出力の直接採用は未合格。再分割/局所補完の制限・可視画素の固定、別原画の比較を続ける。製品未採用であり、現行DINO/SAMを勝手に置換しない。同じ入力・同じ評価条件で候補を比較し、元の可視画素、目口、首/襟、透過、RAM/VRAM/所要時間を記録する。量子化や低解像度化を品質合格と混同しない

- 最優先: SPEC 4.2「素材分割の到達条件」に沿って、目・口・首/襟・腕・髪束の機能単位へ再構成する。現行は6〜7部位の独立テクスチャ・メッシュだが、頂点格子と変位場を共有しており、独立関節や隠れ領域補完まで完成したものではない
- SPEC 4.2「品質優先パイプラインの設計」の構造解析・素材分離・補完・リグ・QAを独立させる。未確定の意味解析/補完方式の比較を先に行い、固定領域の細分化だけで実装を進めない
- Grounding DINO baseの通常経路への組み込みは利用者承認済み。3原画の部位位置候補は比較で改善したが、通常経路の再生成・襟の欠損・実写の顎境界・目口内部素材の検証は必要。Florence-2は比較のみ。専用See-through重みの許諾表示も未確認のまま
- 表示中の読者が一貫した世代を読む原子的世代参照切替を実装する。生成失敗時の旧版保持・公開失敗の復旧・次回生成時の中断復旧は実装済みだが、ディレクトリ切替の短い未配置期間と起動時復旧は未対応
- 首下と襟・胴体の接続、実写以外の閉口線を原画と並べて確認する
- 原画の合わせ目と上下唇メッシュは共通パイプラインへ実装済み。原画にない口内・歯・舌の素材、最大開口と母音間の連続動作を詰める。上まつげ・肌下地・目のクリップ・虹彩分離・虹彩下地の補間は実装済みだが、純粋な白目/下まぶたの独立素材は未解決。補間だけで隠れた目の構造が復元できたと扱わない
- 目の局所SAM解析でむぎの黒い残留線は通常出力・実画面で解消した。閉眼線の太さ・髪際・半閉眼の輪郭、原画の薄い閉口線と開口の造形は引き続き品質確認する。モデルによる髪/顔の競合を全入力で解消したとは扱わない
- 30_live側の既存Anime2.5DRig/Pico書き出しとの整合を確定する。現行Three試作をPico同一形式と呼ばず、30側の未コミット変更を保護する
- 素材ごとの親子・クリップ・隠れ部分補完を保存し、同じ画像を別名コピーして分割の代用にしない

- 左右の目開閉、`MouthOpenY`、`MouthForm` を実装する
- 顔・体・左右腕・前髪・横髪・後髪の小角度モーションを実装する
- 本体とブラウザ確認画面が同じ描画コードを使い、透過を維持する（OBSは対象外）
- 実写・女性A・むぎを同じ処理・設定で通す。キャラ名分岐・専用座標は禁止。女性Aの目検出/閉眼補完、むぎの上半身分類は未解決
- 無音時の閉口、合成母音による6口形切替、まばたき、継ぎ目を実画面で確認する

### T12. 全テストキャラの再生成とスマホ確認

- 現行の比較対象（女性A・むぎ・実写テスト）を同じ2.5D工程で作り直す。実写は実験用で、公式キャラの候補とは分ける
- 正面、顔向き、左右閉眼、6口形、腕、髪揺れを同条件で撮影する
- スマホ用ギャラリーを新成果物へ差し替え、旧3D画像を完成例として残さない

---

## 後続（条件待ち）

- **macOS / Linux 対応**。Windows 先行が固まってから。**CUDA必須の決定により、macOS 対応は別の解（Metal 経由）が要る**
- **動画生成機能**。VRAM 8GB では実用にならないため保留。GPU 要件を上げる判断が出たら再検討する
- **配布ビルドと更新機構**。署名・自動更新の要否は販売方針が決まってから。**ComfyUI 同梱で配布サイズが大きくなる**ため、差分更新の要否も併せて判断する

## 決定済み（2026-09-02 利用者判断）

再検討の対象にしない。変えるなら利用者へ確認する。

| 項目 | 決定 |
|---|---|
| 製品名・識別子 | `LocalVTuberStudio` / `com.localvtuberstudio.desktop` |
| 画像生成バックエンド | **ComfyUI 同梱**（品質優先。`stable-diffusion.cpp` は却下） |
| 動作要件 | **CUDA対応NVIDIA GPU必須**。CPUフォールバックを実装しない |
| 先行製品を上回る改良（[SPEC.md](../SPEC.md) 4.10 A〜D） | 制約内で成立するなら実装してよい |
