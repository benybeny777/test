# AGENTS.md — Claude Code / AI エージェント向け作業ガイド（ルール正本）

このファイルが唯一のルール正本です。`CLAUDE.md` はこのファイルへのリダイレクトのみです。ルールを追加・変更する場合はこのファイルだけを編集してください。

> **作業を引き継いだ場合は [HANDOFF.md](HANDOFF.md) を先に読む。** 未マージの作業と再開方法だけをまとめてあります。恒久ルールはこのファイル、未完了項目は [docs/TASKS.md](docs/TASKS.md)、過去の経緯はPR、という持ち場です。

> このルールは `PicoAgent` の `AGENTS.md` を正本として複製し、本プロジェクト（1枚のイラストからVTuber用モデルを生成し配信するアプリ）向けに調整したものです。共通する運用・設計規則はそのまま引き継ぎ、PicoAgent固有の事実（デスクトップペット、LLMプロバイダ、配布用の別リポジトリ、Live2D／THA3の素材工程）は本プロジェクトの事実へ置き換えています。

## 最重要運用ルール

- **本アプリはクラウドAIサービスへ利用者の素材・音声・会話を送らない。生成も認識も判定もすべて利用者のPC内で完結させる。** モデル生成、表情生成、口形生成、音声認識、話題・感情判定のいずれについても、外部の推論API（OpenAI／Anthropic／Google／Stability／Tripo等の生成API、クラウドASR、クラウドTTS）を呼ぶ実装を追加しない。ネットワークを使ってよいのは (1) 公式配布元からのモデル重み・ランタイム・アプリ更新の取得、(2) 利用者が明示的に開始した外部連携（配信ソフトへのローカル接続を含む）だけとする。「精度が上がるから」「無料枠があるから」はクラウド推論を入れてよい理由にならない。判定に迷う実装は追加前にPR本文へ論点を書き、利用者の判断を仰ぐ。
- **入力イラストの権利は利用者にあることを前提とし、アプリとドキュメントの両方でその前提を明示する。** 取り込み画面と生成開始前に「自分が権利を持つ、または権利者から許諾を得た画像だけを使うこと」を確認させ、確認なしに生成へ進める導線を作らない。他人の作品の取り込みを助ける機能（URLからの自動収集、SNSからの一括取得、権利表示の除去）は実装しない。
- `AGENTS.md` はすべて日本語で記述する。英語セクション・英語コメントは追加しない。既存の英語記述を見つけたら日本語に置き換える。
- ルールに反する記述・命名・コメント・dead code を発見した場合は、その場で修正して作業完了後に事後報告する。都度確認を求めない。
- ルール正本は `AGENTS.md` 1本。`CLAUDE.md` はリダイレクトのみなので編集しない。恒久ルールはすべてこのファイルに追記する。
- **ドキュメントは常に最新を保つ。コード・スクリプト・設定・依存関係・フォルダ構成・コネクタのインターフェースなど、README や SPEC.md に記載のある内容を変更したら、必ず同じ作業内でドキュメントも更新する。ユーザーから指摘されてから直すのは禁止。**
- **大きなファイルは、肥大化を検知したら責務を棚卸しし、分割するか一体のまま保つかを先に判断する。** 行数だけを理由に分けず、唯一の正本、参照方向、外部へ公開する最小インターフェース、機械検査の境界を決め、その境界で保守・検証が独立する場合だけ分ける。分割によって読むファイル、共有状態、相互呼び出しが増えるなら、一体の方が効率的として分けない。単なる行移動、同じ状態の相互書き換え、循環依存、同じ説明の複製になる分割は禁止する。文書は読者と更新理由、コードは状態の所有者と副作用を分割基準にする。判断結果と、分ける場合の境界または分けない理由をPR本文へ残す。
- 設定キー一覧の正本は `docs/SETTINGS.md` とし、記載漏れ・削除漏れは `doc_sync::guard` のテストが実装と双方向で検出する（手作業の突き合わせは不要。設定キーでない `PICOVTUBER_*` 文字列は理由付きで `NOT_CONFIG_KEYS` へ）。`SPEC.md` は設定の保存・反映機構、`README.md` と `MANUAL.md` は利用者がよく使う項目だけを説明し、全キーを複製しない。
- 対象ドキュメントの目安: アーキテクチャ・コネクタ仕様・生成工程の受け渡し形式・設定機構 → `SPEC.md`、全設定キー → `docs/SETTINGS.md`、概要・起動・主要機能 → `README.md`、一連の利用手順 → `MANUAL.md`、開発・xtask・配布ビルド・リポジトリ構成 → `docs/DEVELOPMENT.md`、未完了・条件待ち → `docs/TASKS.md`。変更の履歴はPR本文とGit履歴へ残し、別の変更履歴ファイルを二重管理しない。
- ドキュメント更新が本当に不要な軽微修正の場合のみ、不要と判断した理由を作業完了時に明記する。
- **利用者が「読み取り専用」「編集禁止」と明示していない限り、「レビューして」は修正までの依頼である。指摘を並べて終わりにしない。** 見つけた問題はその作業内で直し、テストまで済ませる。方針判断が要るものも妥当な既定を選び、採否、理由、代替案、検証境界をPR本文へ書く。直さずに残すのは、実機や外部資産が無くてコードでは解消できない場合だけで、再現条件と着手条件を `docs/TASKS.md` へ残す。
- **PR本文を変更記録とレビュー記録の正本にする。** 目的、主な変更、採否と理由、実施した検証、未確認事項をそのPRへ書く。完了した作業を別のMarkdown台帳へ複写しない。恒久ルールは `AGENTS.md`、現行仕様は `SPEC.md`、利用手順は `README.md` または `MANUAL.md`、未完了だけは `docs/TASKS.md` へ移す。
- **PR本文には、別のエージェントが会話履歴なしで引き継げる改修設計を書く。** 利用クレジットの枯渇、セッション切断、担当交代はいつでも起きる。そのとき残るのはPR本文とコードだけで、会話は残らない。**「続きをやって」と言われた別AIが、PR本文だけを読んで同じ判断へ到達できること**を書けたかどうかの判定基準にする。次を必ず含める。
  - **背景と原因**: 何が問題で、なぜそうなっていたか。「直した」ではなく「何が壊れていて、どの経路で起きるか」を書く。再現条件が要るものは条件も書く。
  - **設計判断と却下案**: 採った方式、**採らなかった方式とその理由**。理由が消えると、引き継いだ側が同じ検討をやり直すか、却下済みの案へ戻る。
  - **未完了の作業**: このPRで終わらなかった部分、次にやること、着手条件。条件待ちなら `docs/TASKS.md` へも書く。
  - **検証の状態**: 実行した検証と結果、**この環境では確認できなかったこと**（実機・GUI・GPU・マイク・配信ソフトが要るもの）を分けて書く。引き継いだ側が「確認済み」と誤解しないようにする。
  - **触ってはいけない前提**: その変更が依存している不変条件（ロックの取り方、工程順、他モジュールとの約束など）。知らずに壊されやすいものを名指しする。
  - 作業を途中で終える場合は、PR本文の冒頭へ「未完了。次にやること」を置き、Draftのままにする。ブランチと変更は必ずpushしてから終える。**ローカルにしか無い作業は引き継げない。**
- **実装・配布・検証を簡単にする選択によって、速度、画質、機能、対応機種、互換性、保守性のいずれかを犠牲にする場合は、採用前または判明した時点で利用者へ具体的に伝える。** 「安全」「簡単」「公式資産がある」だけを理由に欠点を隠さず、何がどの環境で劣るか、改善に何が必要か、今回実施するか保留するかをPR本文へ残す。未完了なら `docs/TASKS.md` にも着手条件を書く。簡単な経路を実装しただけで、犠牲にした高速経路や高品質経路まで対応済みと扱わない。**とくに生成品質は「工程が最後まで通ること」と「見られる出来であること」が別物なので、通ったことを品質の合格にしない。**
- **CodexとClaudeは互いを自動起動しない。相互レビューはユーザー操作だけをトリガーとする。** CodexからClaude CLIを呼ぶことも、ClaudeからCodex CLIを呼ぶことも禁止する。ユーザーが対象成果物、レビュー担当、レビューしながら開発する範囲を明示した場合だけ実行し、その指示は指定された同一PR内だけで有効とする。別PR・別作業へ過去の個別指示を持ち越さない。通常の実装・レビューは作業中のエージェント自身が完結させ、別エージェントの応答を完了条件にしない。レビュー結果は同じPR本文へ残す。
- **利用者がClaudeまたはCodexによる設計レビューを行う作業は、実装より先にDraft PRを作る。** 作業エージェントはコードへ着手せず、PR本文へ背景と原因、要件、採用案、却下案と理由、変更対象と不変条件、互換性、検証計画、未確認事項、レビューで判断してほしい論点を書いて作業ブランチをpushする。利用者が作業エージェントへレビュー担当の起動まで明示的に依頼した場合は、Draft PRの作成とpushを終えた後に限り、そのPRを対象として指定された担当を起動してよい。起動依頼が無ければ利用者がレビュー担当へ指示する。いずれもレビュー完了または実装再開の明示があるまで、そのPRのコード実装を進めない。
- コミットメッセージ・ソースコードのコメント（Rust / JS とも）は基本的に日本語で書く（コミットは英語だけの件名にしない）。`feat` / `fix` / `docs` / `chore` などの短い英語プレフィックスや、API / JSON / trait / 識別子・関数名など技術用語の英語はそのまま使ってよい（無理に和訳しない）。
- **大きなファイル容量・配布容量・転送量は、一般的なMBまたはGBだけで見やすく書く。bytesを単独表示または併記せず、外部仕様がMiB／GiB表記の場合も10進のMB／GBへ換算する。** 識別子・実装上のbyte列・検証ロジックの説明は容量報告ではないため対象外とする。
- **常に Codex / Claude（作業エージェント自身）の利用クレジット消費を最小化して作業する。** 不要な長文出力、重複調査、過剰なファイル読み込み、不要な並列探索、同じ確認のやり直しを避ける。まず検索・差分確認・必要最小限のファイル読み取りで判断する。
  - 検証コストの優先順位: まず `cargo check`（コンパイル）→ 必要なら `cargo test --lib`（ロジック）で済ませる。**GUI 起動（`cargo xtask dev`）・生成パイプラインの実走・フルビルドは高コストなので最後の手段**とし、目視確認が本当に必要なときだけ、複数の確認をまとめて 1 回で行う。
  - 目的別PRの最終ローカル検証をまとめるときは `cargo xtask verify` を使う。本体・xtaskの書式、全target静的解析、テスト、共通スキル正本とClaude Code用自動認識ファイルの同期、追跡JavaScript／MJS構文、Git差分を途中失敗後も最後まで実行し、失敗とスキップを末尾で確認する。日常の小修正で毎回フル検証を回す意味ではない。
  - 編集はまとめて行い、**ビルド/テスト/起動は変更が出揃ってから最小回数**だけ実行する。同じ内容のビルド・テスト・起動・読み取りを繰り返さない。
  - `Cargo.toml` / `Cargo.lock` を変更したら `cargo audit --file <Cargo.lock>` で既知脆弱性を確認する。GitHub Actions は無料枠・請求制限による開始前失敗と通知の連発を避けるため使わない。依存更新時と販売前のローカル検証で監査し、保守終了などの情報警告と脆弱性を分けて評価して、対象OS・実行経路と見送り理由をPR本文へ残す。
  - **外部依存は原則として公式LTSまたは長期保守安定版を採用し、LTS相当が無ければ安定版latestを採用する。生成モデル・音声モデルはLTS扱いにせず、ライセンスと再配布条件を確認したうえで公式の最新安定版へ追従する。** Cargo・JavaScript・Pythonのパッケージ、同梱ランタイム、Webランタイム、生成モデル・音声モデルを毎月1日に `.agents/skills/audit-picovtuber-dependencies/SKILL.md` の `all` で点検する。互換性、ライセンス、対象OS、再現可能なハッシュ固定の問題で旧版を維持する場合は理由と確認日をPR本文へ残し、未完了なら `docs/TASKS.md` へ移す。
  - **CUDAを使うPythonランタイムは、Python単体の最新版ではなく、対象GPU、CUDA、PyTorch、CUDA拡張の公式wheelが同時に対応する安定版へ統一する。現在の基準はPython 3.12とする。** 機能ごとにPython本体を重複取得せず、PicoVTuber管理下の同じPython本体を再利用して仮想環境だけを分離する。互換性のため別版が必要な機能は、理由、依存組合せ、移行条件をPR本文と未完了時の `docs/TASKS.md` へ残す。移行検証に成功したら不要になった旧Python本体、仮想環境、取得キャッシュを同じ作業内で回収する。基準版を変える場合は、全CUDA機能の公式wheelが新しいPython ABIへ揃ったことを確認し、起動と実処理を新しい組合せで検証してから切り替える。
  - **大容量のモデル・ランタイム・SDK・GPU向けパッケージは、機能の利用意思とOS・CPUアーキテクチャ・GPU種別・必要能力を取得前に判定し、利用できない物を予備またはフォールバック目的で取得しない。** とくにCUDA版はCUDA対応NVIDIA GPUを確認できた場合だけ取得し、AMD／Intel／GPUなし環境ではCPU版が実際に対応する機能だけCPU版を選ぶ。自動判定を明示指定で上書きできる場合も、非対応構成の既定取得を許さない。全対象の一括取得は `cargo xtask setup all` のような明示操作だけにし、起動・通常ビルド・引数省略へ任意機能の取得を混ぜない。
  - **取得・更新・ビルドの正常終了後は、PicoVTuber管理下の旧版、退避、staging、`.part`、`.incoming`、`*-old-*`、完了済み再開キャッシュを残さない。** 新版のサイズ・ハッシュ・起動可否など必要な検証が成功してから旧版を削除し、失敗時は復旧に必要な物だけを残す。失敗時に残した再開用ファイルも次回成功時に必ず回収する。共有キャッシュ、利用者が指定した外部配置、他アプリの管理領域は削除対象にしない。
  - 起動して確認したら使い終わったプロセスは止め、ロックや再ビルドの無駄を避ける。確認が自動化しづらい（GUI・音声・GPU など）場合は、コードの正しさで判断し、最終目視はユーザーに委ねてよい。
  - **プロセスを止める前に、必ず親プロセスとコマンドラインで「自分が起動したもの」かを確かめる。** `python.exe` や `node.exe` は他アプリも使っており、名前だけで一括終了すると稼働中アプリを壊す。`cargo xtask stop` は、PicoVTuber本体と、コマンドラインがPicoVTuber管理下のパスを含む子プロセスだけをプロセスツリーごと回収する。
- **対応 OS は Windows、macOS、Ubuntu Linux 24.04 LTS x64**。Linuxは `.deb` とAppImageを配布形式とし、実行ファイルだけでなく実パッケージの導入・起動・削除まで検証する。WaylandでOS側の許可やポータルに依存する画面取得・音声取得は、拒否や未提供を成功扱いにせず理由付きエラーを返す。Linux専用依存を変えたら `.claude/hooks/session-start.sh` とREADMEのapt一覧を同じ作業で同期する。
- **開発・検証でPowerShellを使う場合は、PowerShell 7の `pwsh` を既定にする。** 作業開始前に `Get-Command pwsh` で実体を確認し、見つからなければWindows PowerShell 5.1へ黙って降格せず、PowerShell 7の導入が必要な理由を利用者へ伝えてインストール許可を求める。日本語ファイルを確認するときはUTF-8を明示する。表示が文字化けしても、すぐにソース破損と判断せずUTF-8で読み直してから判断する。
- 既に未コミット変更がある場合は、ユーザーまたは別エージェントの作業として扱い、勝手に巻き戻さない。
- 作業中のスクリーンショット・連番フレーム・変換途中のファイルなど、一時的な作成物はリポジトリ直下の `temp/` 内だけに置く。ルートやソースディレクトリへ `tmp-*` 等を作らない。`temp/` は `.gitignore` で除外し、一時作成物をコミットへ含めない。
- **透過出力の成果物に、背景の消し残しと誤認されるハロー・矩形・色面・太い外周影を付けない。** 画像四隅のアルファ値だけで合格にせず、透過市松または配信ソフトの実画面で表示を確認する。意図した背景が必要な配信画面などは、キャラクター描画ではなく画面側の明示的な背景レイヤーとして分離する。
- **画像の引き伸ばし拡大は禁止する。** 縦横のピクセル数だけを増やす nearest／bilinear／bicubic／Lanczos 等の補間、OS画像API、Pillow、ImageMagick、ブラウザ、CSSによる拡大結果を、テクスチャ正本や最終出力へ採用してはならない。入力イラストの正規化は**等倍以下かつ縦横比維持**に限定する。テクスチャ解像度が足りない場合は「高精細」として合格させず、より大きい元絵を利用者へ求めるか、超解像を行うなら実際に細部が増えているかを元絵・生成物・実表示の3点で比較してから採用し、方式と処理前後の寸法をPR本文へ残す。
- **生成モデルの正本と実行用データを分離する。** 生成パイプラインの入力（利用者のイラスト）と各工程の中間成果物は上書き・削除せず、`cargo xtask` またはパイプラインの正規入口だけを通して実行用の `.vrm` とテクスチャを作る。中間成果物を実行用として直接読ませない。
- 作業エージェント向けの再利用手順は `.agents/skills/<名前>/SKILL.md` を正本にする。Claude Code用の `.claude/skills/<名前>/SKILL.md` は同名の共通スキルを読むだけの薄い入口とし、手順を複製しない。
- `.agents/skills/` の共通スキル、付属スクリプト・参照資料、対応する `.claude/skills/` の入口はすべてGit管理対象にする。追加・更新したものをローカル専用や未追跡のまま残さず、実装と同じ目的別PRへ含める。
- **同じ種類の作業が2回以上発生した、工程順を誤ると成果物が壊れる、混入・公開・破壊・課金の危険がある、または複数エージェントへ引き継ぐ作業は、その作業内で共通スキル化する。** 配布ビルド、モデル生成、実画面検証、公開前監査はこの対象とする。単発の小修正や一般知識だけの手順は無理にスキルへしない。
  - 新規作成前に `.agents/skills/` を検索し、目的が重なるスキルがあれば新設せず既存スキルへ統合する。1つの依頼で複数スキルが必要な場合も、責務が重複しない最小構成にする。
  - スキルを使うたびに、記載されたパス、コマンド、版、成果物、公開先を現行コード・文書・公開状態と照合する。古い情報や不足分を見つけたら、そのスキルを同じPRで更新してから作業を続け、過去の手順をそのまま実行しない。
  - `AGENTS.md` などのルール文書に、恒久ルールではなく特定作業の工程・コマンド・成果物一覧が増えており、上の条件からスキルへ分離した方がよい箇所を見つけたら、対象と分離先候補をユーザーへ報告してスキル分離を促す。既存スキルへ統合できる場合は新設を勧めない。
  - スキル名は英小文字・数字・ハイフンだけの動詞始まりとし、`SKILL.md` のfrontmatterは `name` と「何をするか・どの依頼で使うか」を含む `description` だけにする。本文は日本語、命令形、500行未満を目安とし、一般論や作業日記を入れない。
  - 壊れやすい工程は、正規入口、入力条件、禁止経路、成果物の識別方法、失敗時の停止条件、検証、公開前照合まで書く。単なるコマンド一覧にせず、古い成果物や開発用資産を混ぜない条件を明示する。繰り返し組み直す処理は `scripts/` へ置き、説明を重複させない。
  - Claude Code用自動認識ファイルは共通正本を最後まで読む指示だけにし、同名フォルダへ手順・スクリプト・参照資料を複製しない。名前一致、リンク先、frontmatter、自動認識ファイル本文の肥大化は `cargo xtask verify` が機械検査する。
- **修正・作業が完了したら、ユーザーからの個別指示がなくても `main` へのマージまで終わらせる。** 手順は「`git add -A` で全ステージ → コミット → 作業ブランチへプッシュ → PR 作成 → squash merge」。自分が編集したファイルだけでなく、既に存在していた未コミット変更も同じコミットに含める（コミットメッセージは日本語。本ファイルの他ルールに従う）。**止めるのは、利用者が「マージしない」「PRだけ」と明示した場合と、破壊的・不可逆な操作を伴う場合だけ**。判断に迷って作業を宙ぶらりんにしない。
- **すべての変更はPRで行い、`main` へ直接pushしない。** 作業開始時に `origin/main` から目的別ブランチを作り、同じ目的の変更だけを1つのPRへまとめる。無関係な修正は別ブランチ・別PRへ分ける。公開済みコミットの書き換えやforce pushはしない。
- **PR のマージは squash merge を使う。** 1 PR = 1 コミットになり `main` の履歴が読みやすい、というだけの理由。
- **作業ブランチを残さない。** マージまで終えたら、その場でリモートとローカルの両方を消す。リモートを消すコマンドはローカルまで消さないので、`git switch main && git pull --ff-only` のあとに `git branch -d` まで行う。残すのは `main` と、いま作業中のworktreeがチェックアウトしている枝だけにする。
  - **squash merge したブランチは `git branch --merged` で検出できない。** 1コミットへ潰されて親子関係が残らないため、「未マージ」に見える枝が実際は取り込み済み、ということが起きる。判定はブランチ名や `--merged` ではなく、**その枝の中身が現行 `main` に存在するか**で行う（関数名・定数・記録の見出しなど、枝を特徴づける文字列を `main` 側で検索する）。
  - **中身が `main` に無い枝を、確認せずに消さない。逆に、古い枝を確認せずにマージもしない。** 分岐元が古い枝をそのままマージすると、完了済みの作業指示と旧実装を `main` へ戻す。取り込む価値のある差分が残っているなら、枝をマージするのではなく現行 `main` から作り直した目的別PRへ移植する。
  - 消した枝と、消してよいと判断した根拠は、その整理を行うPR本文へ残す。次に見た人が同じ照合をやり直さずに済む。
- **製品版番号はSemVerで管理し、手作業で複数箇所を書き換えない。** 通常の修正・依存／モデル／同梱物更新・文書は同じ `major.minor` 系列の `patch` を自動加算する。`minor` と `major` はユーザーから版を上げる明示指示があった場合だけ変更し、エージェントが機能内容から独断で上げない。`cargo xtask bump-version <major|minor|patch>` を正規入口にし、アプリCargo・xtask Cargo・Tauri設定を同期する。同じ版のRelease資産は上書きしない。判定した種別と理由をPR本文へ残す。

## エラー処理・リソース

- **実害のあるバグは見つけ次第その場で直す。** 別の作業中に見つけた場合も「今回の目的外」で保留にしない。実害とは、利用者のデータが消える・機能が黙って動かなくなる・誤った内容を見せる・費用や時間を無駄にさせる、のいずれかを指す。目的が違うなら別ブランチ・別PRへ分けてよいが、**分けることと後回しにすることは違う**。同じ作業内で直しきり、判定をPR本文へ残す。すぐ直せない場合だけ、再現条件、影響範囲、着手条件を `docs/TASKS.md` へ積む。
- **利用者から見える失敗を、ログだけで済ませない。** ログは利用者が見る場所ではない。生成ジョブの中断、マイク入力の途絶、配信出力の停止のように「できている」と思い込む機能が失敗したら、画面へ理由を出す。うるさくならないよう原因ごとに1度だけ知らせ、復旧したら次の失敗はまた知らせる。
- **エラーは原則 `Result` で上位へ伝播させる。サイレントスキップ・無言フォールバック・無言デフォルト降格は禁止。** フォールバックを書く場合はその箇所に `// フォールバック許可: 理由` コメントを付ける。
  - 例外として、ランタイムの `is_up()` のような「接続可否を返すプローブ」は、未起動・タイムアウトを `false` に正規化してよい（接続できないこと自体が戻り値なので、握りつぶしではない）。
  - **生成工程の失敗を、見た目だけ整った成果物で隠さない。** 側面・背面が作れなかった、口形が作れなかった、といった欠損は、白紙や正面の複製で埋めずに工程の失敗として返す。利用者が配信本番で初めて気づく作りにしない。
- **状態の「読む → 直す → 書き戻す」は `store::lock_state(名前)` を取ってから行う**（設定にJSON文字列で持つ一覧＝生成ジョブ・表情プリセット・配信プロファイル等、およびモデル一覧のようなファイル状態）。生成パイプラインは複数工程を並行して進めるため、ロックが無いと同じ一覧を触る処理同士が互いの追加を上書きして消す。ロックを保持したまま `.await` しないこと（状態更新は同期処理で完結させる）。`store::guard` のテストが、ロックを取らない書き戻しを検出して失敗させる。
- **「壊れていたら空で継続」するフォールバックを書いたら、その空データをそのまま保存し返さないこと。** 破損時に空で開始し、次の保存で書き戻すと**ユーザーのデータが恒久的に消える**。設定にJSON文字列で持つ一覧は `store::load_list()` で読み、読めなければ**空一覧にせずエラーを返して書き戻しを止める**。ファイルで持つ状態は `store::quarantine_corrupt()` で破損ファイルを退避してから空で続ける。`store::guard` のテストが、退避もエラー返却も伴わない「空で置き換え」を検出して失敗させる。
- `unwrap()` / `expect()` は、起動時の致命的初期化（`main`/`setup`）や Mutex ロックなど「失敗＝即異常終了が妥当」な箇所に限る。通常経路では `?` と `Result` を使う。
- **取得したリソースは明示的に解放する。** 子プロセス・ファイルハンドル・ネットワーククライアント／接続・音声ストリーム・タイマー（`setInterval` 等）・イベントリスナ・フロントの `URL.createObjectURL` / `AudioContext` / WebGLコンテキストなど、確保したものは「正常終了・失敗・中断（キャンセル）」のどの経路でも確実に解放する。`Result` の早期 return や例外経路で解放が漏れないようにする。
  - 子プロセスは終了時に `kill` し、`wait` でハンドルまで回収する（ゾンビ・ハンドルリークを残さない）。
  - `reqwest` のプローブ系には `timeout` を付ける（接続が永久に滞留しないようにする）。長時間ストリーミング（モデルDL・生成）は全体timeoutを付けない代わりに接続自体の滞留に注意する。
  - フロントの `URL.createObjectURL` は再生終了・失敗・中断で必ず `revokeObjectURL` する。`setInterval` 等のタイマーは全経路で `clearInterval` する。**three.js のジオメトリ・マテリアル・テクスチャはモデル切替時に `dispose()` する**（GPUメモリは GC では返らない）。
- **PicoVTuber は同時に1つしか動かさない**（`tauri-plugin-single-instance`）。`store::lock_state` はプロセス内 Mutex でプロセスをまたぐと効かず、設定・モデル一覧・生成ジョブはどれも「全体を読んで全体を書き戻す」ため、2つ目のインスタンスがあると片方の保存が古いスナップショットで丸ごと上書きされる（＝利用者のデータが黙って消える）。マイクと配信出力も二重に掴む。2つ目の起動は既存ウィンドウを前面へ出して終了する。**この前提を外す変更（複数ウィンドウ・複数プロセス）をするなら、状態ファイルのプロセス間排他を先に用意すること。**
- ユーザーから明示指示がない限り、同梱実行ファイル・同梱モデル・同梱ランタイムは PicoVTuber 管理下の専用ディレクトリ（原則 `src-tauri/resources/bundled/<機能名>/`、実行時データは `app_data_dir` 配下）に設置し、既存の手動インストール先・グローバルPATH・共有ポートを既定で使わない。手動インストールを使う場合は設定で明示されたときだけ優先し、同梱物のアンインストール・一時ファイル削除は PicoVTuber 管理下のパスだけを対象にする。

## 設定値・シークレット

設定項目を追加・変更・削除する作業は、先に `.agents/skills/add-picovtuber-setting/SKILL.md` を読む。以下は個別手順ではなく、すべての設定実装へ常時適用する安全規則とする。

- 設定の正本は `src-tauri/src/config.rs`（永続ファイル `app_config_dir/config.json`）。優先順位は **永続ファイル > 環境変数 > ハードコード既定値**。新規設定で環境変数を「正本・保存先」にしてはならない（環境変数は既定値としてのみ参照する）。
- 数値・閾値・パス・モデル名など「設定として変えうる値」はソースに散らさず `cfg.get(キー, 既定値)` で都度読む。コネクタは自分の設定項目を `config_schema()`（`Vec<Field>`）で申告し、設定画面に自動で並ぶようにする。新しい設定キーを足したら `docs/SETTINGS.md` に必ず記載し、利用者が日常的に触るキーなら README と MANUAL も更新する。設定の保存・反映方式を変えた場合は SPEC も更新する。
- 設定画面が唯一のGUI設定入口。`ui/settings.{html,css,js}` と `commands.rs` の `settings_*` command で構成し、保存後は実行中に反映する（コネクタは `cfg.get()` で都度読むため再起動不要）。
- **本アプリはクラウド推論を行わないため、外部サービスのAPIキー設定を新設しない。** それでも配信ソフト連携やローカルAPIの認証で秘密値を持つ場合は、必ず `type="password"` で申告し、キー名に `API_KEY` / `TOKEN` / `SECRET` / `PASSWORD` / `WEBHOOK` / `CREDENTIAL` のいずれかを含める。暗号化の要否は「スキーマでの password 申告」に加えて**キー名でも判定する**（`secret::is_secret_key`）。スキーマに載らない設定項目——設定画面が直接組み立てるフィールドなど——は型申告だけでは拾えないため。`secret::guard` のテストが (1) コネクタのスキーマで秘密っぽいキーが password 型でない申告、(2) 設定画面の password 項目が `is_secret_key` に載らない名前、の両方を検出して失敗させる。
- 設定スキーマで `type="password"` の値は平文で `config.json` に置かない。OSの資格情報保護（`src-tauri/src/secret.rs`）へ預ける。Windows は DPAPI で暗号化して `enc:dpapi:v1:` 形式、macOS はログインキーチェーンへ預けて `enc:keychain:v1:<設定キー>` 形式で保存し、`config.get()` が読み取り時に透過的に復号する（同一PCの同一OSユーザーのみ復号可能）。**復号できないまま保護形式が残る値（別PC・別OSユーザー・別OSの `config.json` を持ち込んだ場合）は `config.get()` が未設定として扱う。** Linux は同等のOS機能を使っていないため平文保存（フォールバック許可）。
- ログ・エラーメッセージ・ドキュメントに秘密値を出さない。`config.json` はローカル個人PC専用で、リポジトリにコミットしない。
- **利用者のイラスト・音声・生成物のパスや内容を、診断ログへ本文ごと書かない。** 記録するのは工程名・所要時間・寸法・失敗理由に留め、画像そのものや音声そのものをログへ落とさない。

## コネクタ開発ルール（このプロジェクトの中核）

生成工程または配信出力コネクタを追加・変更する作業は、先に `.agents/skills/add-picovtuber-connector/SKILL.md` を読む。以下は個別手順ではなく、すべてのコネクタへ常時適用する設計規則とする。

PicoVTuber は「1枚絵をモデルに変える生成工程（Stage）」と「できたモデルを配信へ出す出力（Output）」をコネクタとして拡張する。**どちらも決まった場所へファイルを置けば自動登録される**。`build.rs` が `src/pipeline/stages/*.rs` と `src/studio/outputs/*.rs` を走査して `#[path] pub mod` を生成し、各ファイルの `inventory::submit!` で登録されるため、中央の `mod.rs` 編集は不要。

- **生成工程コネクタ** は `src-tauri/src/pipeline/stages/` に置き、`Stage` トレイトを実装して `inventory::submit! { StageReg { make: || Box::new(<型>) } }` で登録する。`run()` は `StageContext`（作業ディレクトリ・設定・進捗通知）を受け取り、`StageOutput`（生成した成果物のパスと寸法）を返す。工程固有の外部ランタイム呼び出しは各コネクタ内に閉じ込め、`pipeline/mod.rs` に漏らさない。
- **工程の依存関係は `requires()` で申告し、実行順をハードコードしない。** `pipeline::plan()` が申告から順序を決める。順序を配列で直書きすると、工程を1つ足すたびに離れた場所を直すことになり、実際に順序と依存がずれた状態が生まれる。循環と欠落は `pipeline::guard` のテストが検出する。
- **各工程は入力を破壊しない。** 受け取った成果物を上書きせず、自分の出力を新しいファイルとして作業ディレクトリへ置く。途中で失敗した工程の出力は次の工程へ渡さない。再実行は「その工程から後ろだけ」をやり直せること。
- **配信出力コネクタ** は `src-tauri/src/studio/outputs/` に置き、`Output` トレイトを実装して `inventory::submit! { OutputReg { ... } }` で登録する。`start()` / `stop()` は冪等にし、二重起動・二重停止で失敗しない。
- **外部プロセスの起動は `tools::process::command()` を使う**（`Command::new` を直接使わない）。Windows のコンソール窓抑止（CREATE_NO_WINDOW）・中断時の子プロセス回収（kill_on_drop）・stdio の既定を1箇所に集約している。実行は `process::output()` / `output_text()` でタイムアウト付きにする。**`tokio::time::timeout(..., command.output())` で自前に包むのは禁止**。上限は付くが、打ち切りで届くのは `kill_on_drop` による直下の子までで、起動直後に本体を立て直す実行ファイルや、さらに子を産む Python ランチャが残る。共通層は `terminate_process_tree()` でツリーごと回収する。常駐プロセスのように future の終了後も動かし続けるものは同期版の `process::command_sync()` を使う。`tools::process::guard` のテストが直接起動と自前 `hide_window` を検出する。
  - **一度きりの実行（起動 → 出力回収 → 終了待ち）を自前で組み立てない。** 同じ処理を書き直すと出力回収の待ち方だけが抜ける。進捗表示や中断が要る長時間処理で `child` を自分で持つ場合も、**出力回収の待ちは `process::join_capture()` を通す**（上限を超えたら中断して戻る）。`tools::process::guard` が `timeout(..., wait())` の自前実装を検出する。
- **ストリーミング受信のバイト列は `text::Utf8Stream` を通してデコードする**（チャンクごとに `String::from_utf8_lossy` しない）。生成スクリプトの進捗出力もHTTPのチャンクも文字の途中で切れるため、直接 lossy 変換すると「まだ続きが来ていないだけ」の末尾バイトが `\u{FFFD}` に置き換わって確定し、日本語のメッセージに文字化けが混ざる。`text::guard` のテストが検出する。
- **文字列の切り詰めは `text::preview()` / `text::truncate_bytes()` を使う**（`&s[..120]` のようなバイト添字で切らない）。Rust の文字列は UTF-8 バイト列なので、位置が文字の途中だと **panic** する。日本語は1文字3バイトのため固定長で切ればほぼ必ず境界を外す。`text::guard` のテストが固定長のバイト添字を検出する。
- **アプリの状態ファイル（設定・モデル一覧・ジョブ・集計など）の書き込みは `store::write_atomic()` / `store::write_json_atomic()` を使う**（`fs::write` で直接上書きしない）。状態ファイルは「全体を読んで全体を書き戻す」更新なので、直接上書きすると書き込み中の異常終了で中身が途中までのファイルが残る。共通層は「一時ファイル → `sync_all` → `rename`」で更新するので、どの瞬間に落ちても完全な旧版か新版のどちらかしか残らない。`store::guard` のテストが `fs::write` の直接使用を検出する（例外は理由付きの allowlist のみ）。
- **ユーザー由来のパスは必ず `permissions::resolve(cfg, input)` で `SafePath` に変換してから使う**。`SafePath` は許可判定を通過したパスを表す型で、生成経路が `resolve` / `SafePath::join` / `SafePath::sibling` に限られている（派生パスも `..` による脱出を防ぐため再検証される）。ファイル操作は `permissions::fs`（`read_to_string` / `read` / `write` / `create_dir_all` など）を使う。アプリ自身が決めた内部パス（作業ディレクトリ等、ユーザー入力を含まないもの）だけ `SafePath::app_owned` を使ってよい。**許可判定を各コネクタで再実装しない**（リンク実体検査・追加許可ルート・拒否パス判定が抜ける）。`permissions::guard` のテストがコネクタを走査して、自前の許可判定とパス引数の `resolve` 漏れを検出し失敗させる。新しいコネクタで独自のルート設定キーを作らず、`PICOVTUBER_ALLOWED_ROOT` / `PICOVTUBER_ALLOWED_PATHS` / `PICOVTUBER_DENIED_PATHS` を使う。`C:\Windows` / `Program Files` は permissions が常に拒否する。
- **許可済みパスへの書き込みは `permissions::fs::write()` を使う**（`SafePath` は `AsRef<Path>` なので `std::fs::write(&safe_path, ..)` がそのまま書けてしまい、ファサードの親フォルダ自動作成と統一エラーメッセージを素通りする）。例外は理由付きの allowlist のみ。`permissions::guard` が検出する。
- 利用者のファイルを削除相当の扱いにする場合、物理削除は禁止し、必ずOSのゴミ箱へ移動する。内部一時ファイル、ログローテーション、ビルド生成物、同梱物アンインストールなど PicoVTuber 管理下の掃除はこの限りではない。
- **設計ガード（`mod guard`）を書くときは走査範囲を自前で決めず、`crate::guard_scan` の `all_sources()` / `stage_sources()` を使う。** 各ガードが独自にディレクトリを歩くと走査範囲がずれ、同じ規則を破っているファイルを1つも検出できない状態になる。**ガードの穴はガードが無いのと同じ。** `guard_scan::guard` のメタガードが自前走査を検出して失敗させる。
- コネクタが設定値を必要とするなら `config_schema()` で申告する。値はフィールドに固定せず**メソッド内で `cfg.get()` から都度読む**（設定画面での変更を再起動なしに反映するため）。
- コネクタを追加・変更したら `SPEC.md` のインターフェース定義と一覧、`docs/SETTINGS.md` の設定キー、必要なら `README.md` と `MANUAL.md` の利用手順を更新する。
- 工程間の受け渡し形式（`StageOutput` とアセットの命名規約）を崩さない。詳細は [SPEC.md](SPEC.md) を参照。

詳細仕様は [SPEC.md](SPEC.md) を参照。ここには作業上の注意点だけを書く。

## プロジェクト概要

PicoVTuber は、**1枚のイラストから配信用のVTuberモデル（VRM）を作り、そのまま表情とリップシンク付きで配信へ出す** Tauri デスクトップアプリ。バックエンドは Rust、フロントは webview（`ui/`、three.js + three-vrm）。生成は工程コネクタ、配信出力は出力コネクタとして差し替え・追加できる。**推論はすべて利用者のPC内で完結し、クラウドAIサービスへは何も送らない。**

## 実行環境

- Windows / macOS / Ubuntu Linux 24.04 LTS を想定。**Rust（cargo）と Tauri CLI**（`cargo install tauri-cli --version '^2'`）、WebView2（Windows ランタイム）。**開発タスクは `cargo xtask`。**
- **Node.js は「アプリと開発タスクの依存」としては使わない。ただし作業用ツールとしては使ってよい。** 判断基準は「アプリのビルド・起動・配布に Node が要るようになるかどうか」で、`package.json` を置いて依存を増やす形にはしない。
- 開発起動: `cargo xtask setup viewer`（three.js / three-vrm 取得）→ `cargo xtask dev`。製品ビルド: `cargo xtask build`（`src-tauri/target/release/bundle/`）。
- **生成工程のうち機械学習を使うもの（`segment` / `multiview` / `mesh`）は、PicoVTuber管理下の専用 Python 環境で動く。** `cargo xtask setup models` が公式配布元から重みを `app_data_dir/models/` へ取得する。GPUが無い環境ではCPU版で動く工程だけを有効にし、動かない工程を「成功」と表示しない。
- 生成モデルの重み・three.js ランタイム・Python 環境はリポジトリに含めない（`.gitignore` 済、`cargo xtask setup` で取得）。
- **Claude Code on the web（Linux コンテナ）で作業する場合は `.claude/hooks/session-start.sh` が環境を用意する。** clone 直後は (1) Tauri / cpal が要求するシステムライブラリが無い、(2) `frontendDist` が指す `ui-dist/` が Git 管理外で存在せず `tauri::generate_context!` がコンパイル時に落ちる、の2点で `cargo check` すら通らない。フックが apt 導入と `build-ui` を済ませる。手で毎回入れ直さないこと。Windows / macOS のローカル環境では `CLAUDE_CODE_REMOTE` を見て何もしない。
  - コンテナには GPU もマイクも配信ソフトも無い。生成パイプラインの実走・リップシンクの実音声・配信出力の目視確認はローカル環境で行う。Rust コードの `cargo check` / `cargo clippy` / `cargo test --lib` は素材無しで通る。

## 製品化（配布）

- `src-tauri/tauri.conf.json` の `productName` / `identifier=com.picovtuber.desktop` / `version` を保つ。アイコンは `src-tauri/icons/`（`npx tauri icon <1024pxソース>` で再生成）。同じコマンドが作る `icons/ios/` `icons/android/` はデスクトップ3OSでは参照されないため、`.gitignore` で追跡しない。
- Windows バンドルはNSIS、Ubuntu Linuxは `.deb` とAppImage。初期配布はWindows先行。**第三者の生成モデル・ランタイムをPicoVTuberのReleaseへ再配布しない**（ライセンス条件が個別に異なるため、公式配布元から利用者のPCへ取得する）。同梱・取得するモデルのライセンスと出所は `THIRD_PARTY_NOTICES.md` へ記載する。

## 主要ファイル

| ファイル | 役割 |
|---|---|
| `src-tauri/src/main.rs` / `lib.rs` | エントリ・Builder（state・command・setup） |
| `src-tauri/src/commands.rs` | Tauri command（`pipeline_*` / `studio_*` / `settings_*`）と共有状態 `AppState` |
| `src-tauri/src/config.rs` | 設定の正本（`app_config_dir/config.json`）。`get()` でシークレットを透過復号 |
| `crates/picovtuber-core/` | **非公開の内部共通コア**。UTF-8安全処理・口形/表情の値域など、Tauri・OS・製品設定に依存しない処理を置く。外部公開は行わず `publish = false` を保つ |
| `src-tauri/src/pipeline/mod.rs` | **生成工程の `Stage` トレイト・レジストリ・実行計画**。`requires()` の申告から実行順を決め、循環と欠落を検出する |
| `src-tauri/src/pipeline/job.rs` | 生成ジョブの永続化と再開（`app_data_dir/jobs.json`）。工程単位で「どこまで終わったか」を持ち、アプリ再起動後は未完了工程から再開する |
| `src-tauri/src/pipeline/stages/*.rs` | 生成工程コネクタ（`.rs` を足すと自動登録）。`preprocess` / `segment` / `multiview` / `mesh` / `rig` / `expression` / `viseme` / `pack` |
| `src-tauri/src/pipeline/runtime.rs` | ML工程が使うPython環境と重みのライフサイクル（取得・検証・起動・進捗・キャンセル）。**外部推論APIは呼ばない** |
| `src-tauri/src/vrm.rs` | VRM 1.0 の組み立てと検証（humanoidボーン・ブレンドシェイプ・口形の必須項目を欠けたまま書き出させない） |
| `src-tauri/src/studio/mod.rs` | 配信モードの状態機械（モデル読み込み → 入力開始 → 出力開始）と `Output` トレイト・レジストリ |
| `src-tauri/src/studio/lipsync.rs` | **マイク入力から口形6種を決めるローカルDSP**。短時間フレームのバンドエネルギーとゼロ交差から母音を推定し、無音は `n`（閉じ）へ落とす。音声はPC内で処理し保存も送信もしない |
| `src-tauri/src/studio/expression.rs` | 表情プリセットとブレンド重み。プリセット（喜／怒／驚／悲／楽）とテキスト指定を同じ重みベクトルへ落とす |
| `src-tauri/src/studio/local_ai.rs` | ローカルAIによる表情自動切替（同梱の音声認識と小型LLMで話題・感情を判定）。**未導入なら機能を無効と表示し、クラウドへ代替しない** |
| `src-tauri/src/studio/outputs/*.rs` | 配信出力コネクタ（`.rs` を足すと自動登録）。`transparent_window`（OBSウィンドウキャプチャ用の透過窓）／`virtual_camera` |
| `src-tauri/src/permissions.rs` | **全コネクタ共通のファイル許可の正本**。`resolve()` が許可判定を通した `SafePath` を返し、`permissions::fs` が `SafePath` 専用のファイル操作を提供する |
| `src-tauri/src/store.rs` | **アプリ状態ファイル永続化の共通層**。`write_atomic()` / `write_json_atomic()` が原子的に書き換え、`quarantine_corrupt()` が読めなかったファイルを退避する |
| `src-tauri/src/text.rs` | **文字列を安全に扱う共通層**。`preview()` / `truncate_bytes()` / `Utf8Stream` |
| `src-tauri/src/secret.rs` | シークレット（`password` 型設定）をOSの資格情報保護へ預ける（Windows は DPAPI、macOS はログインキーチェーン）。Linux は平文 |
| `src-tauri/src/doc_sync.rs` | **ドキュメントと実装のズレを検出する設計ガード**（テスト専用）。実装が参照する `PICOVTUBER_*` がすべて `docs/SETTINGS.md` に載っているか、逆に一覧だけに残った削除漏れが無いかを双方向で照合する。工程一覧と SPEC の**工程分類表**も同じく双方向で照合する |
| `src-tauri/src/guard_scan.rs` | **設計ガードの走査範囲の正本**（テスト専用）。`all_sources()` がバックエンド全ソースを返す |
| `src-tauri/src/tools/process.rs` | **外部プロセス実行の共通層**。`command()` が Windows のコンソール窓抑止・`kill_on_drop`・stdio 既定を適用し、`output()` / `output_text()` がタイムアウト付きで実行し、打ち切り時はプロセスツリーごと回収する |
| `scripts/` | ML工程が呼ぶ Python スクリプト（分割・多視点・メッシュ化）。繰り返し組み直す処理はここへ置き、説明を重複させない |
| `xtask/` | 開発タスクランナー（`cargo xtask setup / dev / build / verify / stop / bump-version`） |
| `.claude/hooks/session-start.sh` | **Claude Code on the web のセッション開始フック**。Linux コンテナで `cargo` が通る状態を作る（apt 導入 → `build-ui` で `ui-dist/` 生成 → 依存取得）。冪等・非対話で、`CLAUDE_CODE_REMOTE!=true` のローカル環境では即 exit する |
| `.agents/skills/<名前>/SKILL.md` | Codex・Claude Codeなど複数エージェントで共有する作業スキルの正本。Claude Codeの自動認識入口は `.claude/skills/` に置くが、実手順はここだけを更新する |
| `docs/TASKS.md` | **未完了・条件待ちだけを置く一覧**。完了結果はPRへ残し、一覧から削除する |
| `ui/index.html` / `studio.js` / `styles.css` | 配信ビュー（透過・three-vrm 描画・口形と表情の反映） |
| `ui/wizard.{html,js}` | 生成ウィザード（取り込み → 権利確認 → 工程進捗 → 確認 → 保存） |
| `ui/settings.{html,js,css}` | 設定ウィンドウ（全設定のGUI編集） |

## フォルダ構成

```text
PicoVTuber/
├── crates/picovtuber-core/     ← 非公開の内部共通コア（publish=false）
├── ui/                         ← webview フロント正本（編集対象）
│   ├── index.html / studio.js / styles.css       ← 配信ビュー
│   ├── wizard.html / wizard.js                   ← 生成ウィザード
│   ├── settings.html / settings.js / settings.css ← 設定ウィンドウ
│   └── vendor/                 ← three.js / three-vrm（git 管理外、setup viewer で取得）
├── ui-dist/                    ← Tauri frontendDist（git 管理外、build-ui で生成）
├── src-tauri/
│   ├── Cargo.toml / build.rs / tauri.conf.json
│   ├── capabilities/default.json   ← 権限（ACL）
│   ├── icons/                      ← 生成アイコン一式
│   ├── resources/bundled/          ← 同梱物（git 管理外、cargo xtask setup で取得）
│   └── src/
│       ├── main.rs / lib.rs / commands.rs
│       ├── config.rs / store.rs / text.rs / secret.rs / permissions.rs
│       ├── guard_scan.rs / doc_sync.rs / vrm.rs
│       ├── tools/process.rs
│       ├── pipeline/
│       │   ├── mod.rs / job.rs / runtime.rs
│       │   └── stages/*.rs     ← 生成工程（.rs を足すと自動登録）
│       └── studio/
│           ├── mod.rs / lipsync.rs / expression.rs / local_ai.rs
│           └── outputs/*.rs    ← 配信出力（.rs を足すと自動登録）
├── xtask/                      ← 開発タスクランナー
├── scripts/                    ← ML工程が呼ぶ Python スクリプト
├── .agents/skills/             ← 複数の作業エージェントで共有する手順スキルの正本
├── .claude/                    ← Claude Code の設定、共通スキル自動認識ファイル、セッション開始フック
├── AGENTS.md / CLAUDE.md       ← ルール正本 / リダイレクト
├── README.md / MANUAL.md / SPEC.md / THIRD_PARTY_NOTICES.md
├── docs/SETTINGS.md            ← 全設定キーの唯一の一覧
├── docs/DEVELOPMENT.md         ← 開発・xtask・配布ビルド・リポジトリ構成
├── HANDOFF.md                  ← 未マージ作業の短い引き継ぎ
└── docs/TASKS.md               ← 未完了・条件待ちだけを置く一覧
```
