# AGENTS.md — Codex / Claude Code など作業エージェント向けルール（正本）

このファイルが唯一のルール正本。`CLAUDE.md` はこのファイルへのリダイレクトのみ。ルールを追加・変更する場合はこのファイルだけを編集する。

- 現行仕様は [SPEC.md](SPEC.md)
- 本家の実測仕様は [docs/REVERSE.md](docs/REVERSE.md)（**実装完了後に削除する。下記「リバース資料の扱い」参照**）
- 未完了作業と着手順は [docs/TASKS.md](docs/TASKS.md)
- 全設定キーは [docs/SETTINGS.md](docs/SETTINGS.md)
- 開発・ビルド手順は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)

## 最重要運用ルール

- `AGENTS.md` はすべて日本語で記述する。英語セクション・英語コメントを追加しない。
- ルールに反する記述・命名・コメント・dead code を発見したら、その場で修正して作業完了後に事後報告する。都度確認を求めない。
- **ドキュメントは常に最新を保つ。** コード・スクリプト・設定・依存関係・フォルダ構成・データ形式など `SPEC.md` や `README.md` に記載のある内容を変更したら、必ず同じ作業内でドキュメントも更新する。利用者から指摘されてから直すのは禁止。
- 対象ドキュメントの持ち場: アーキテクチャ・データ形式・パイプライン → `SPEC.md`、全設定キー → `docs/SETTINGS.md`、概要・起動・主要機能 → `README.md`、開発・ビルド・配布 → `docs/DEVELOPMENT.md`、未完了・条件待ち → `docs/TASKS.md`。変更の履歴は PR 本文と Git 履歴へ残し、別の変更履歴ファイルを二重管理しない。
- ドキュメント更新が本当に不要な軽微修正の場合のみ、不要と判断した理由を作業完了時に明記する。
- **利用者が「読み取り専用」「編集禁止」と明示していない限り、「レビューして」は修正までの依頼である。** 指摘を並べて終わりにしない。見つけた問題はその作業内で直し、テストまで済ませる。方針判断が要るものも妥当な既定を選び、採否・理由・代替案・検証境界を PR 本文へ書く。
- **実装・配布・検証を簡単にする選択によって、速度・画質・機能・対応機種・互換性・保守性のいずれかを犠牲にする場合は、採用前または判明した時点で利用者へ具体的に伝える。** 「安全」「簡単」だけを理由に欠点を隠さず、何がどの環境で劣るか、改善に何が必要かを書く。
- **先行製品を下回る点・上回る点は、判明した時点で必ず利用者へ伝え、[SPEC.md](SPEC.md) 4.10 の表を更新する。** 「クラウドをローカルへ置き換える」以上、必ず劣る部分が出る。ここを曖昧にしたまま進めると完成間際に手戻りする。**楽観的な見込みのまま放置せず、実測値が出たら置き換える。** 良い知らせだけを報告しない。
- コミットメッセージとソースコードのコメント（Rust / JS / Python とも）は基本的に日本語で書く。`feat` / `fix` / `docs` / `chore` などの短い英語プレフィックスや、API・JSON・trait・識別子・関数名などの技術用語は英語のままでよい。
- **大きなファイル容量・配布容量・転送量は十進の MB / GB で書く。** バイト数の単独表示や併記をせず、外部仕様が MiB / GiB 表記でも 10 進へ換算する。
- 既に未コミット変更がある場合は、利用者または別エージェントの作業として扱い、勝手に巻き戻さない。
- 作業中のスクリーンショット・連番フレーム・変換途中のファイルなど一時的な作成物は `temp/` 内だけに置く。ルートやソースディレクトリへ `tmp-*` 等を作らない。`temp/` は `.gitignore` で除外し、一時作成物をコミットへ含めない。
- **常に作業エージェント自身の利用クレジット消費を最小化する。** 不要な長文出力、重複調査、過剰なファイル読み込み、不要な並列探索、同じ確認のやり直しを避ける。まず検索・差分確認・必要最小限の読み取りで判断する。
  - 検証コストの優先順位: `cargo check` → 必要なら `cargo test --lib` → **GUI 起動・生成の実走・スクリーンショットは高コストなので最後の手段**とし、目視確認が本当に必要なときだけ複数の確認をまとめて1回で行う。
  - 編集はまとめて行い、**ビルド・テスト・起動は変更が出揃ってから最小回数**だけ実行する。

## このプロジェクト固有の最重要規則

### 製品はクラウドAPIを呼ばない

- **これは本製品の存在理由である。** 生成・認識・会話・描画のすべてを利用者のPC内で完結させる。製品の実行時にネットワークへ出るのは、モデルとランタイムの初回ダウンロードだけに限る。
- 「とりあえずクラウドで動かして後でローカルに置き換える」を暫定実装として入れない。ローカルで動かないなら、その機能は未実装として `docs/TASKS.md` へ残す。
- **生成経路で外向き通信が発生しないことを機械検査するテストを置く。** モデルダウンローダ以外に HTTP クライアントを生成する箇所を検出して失敗させる。例外は理由付きの allowlist のみ。
- 参考にした先行製品はクラウドAPIの寄せ集めで成立している（[docs/REVERSE.md](docs/REVERSE.md)）。**その構成をそのまま採用しない。** 各工程のローカル置換が本プロジェクトの中身である。

#### ローカルで成立しないと判明した場合

**黙ってクラウドを入れない。黙って諦めて機能を削らない。利用者へ提案して判断を仰ぐ。**

どの工程も、実測の結果ローカルでは品質・速度・VRAMのいずれかが成立しないと分かることがある。そのときは作業を止めて、次を揃えて報告する。

- **何がどう成立しなかったか**（実測値で示す。「品質が低い」ではなく、何がどれだけ足りないか）
- **試した緩和策と、それでも届かなかった理由**
- **クラウドを使う場合の具体案** — どのサービスのどの機能を、どの工程だけに使うか。1回あたりの概算費用、利用者に必要になる手続き（アカウント・APIキー）、失われるもの（オフライン動作・プライバシー・無料であること）
- **クラウドを使わない代替案** — 機能を削る、品質を落とす、GPU要件を上げる、など。それぞれ何を失うか

**判断は利用者が行う。** 採用が決まったら、その工程だけをクラウドへ出す設計にし、他の工程はローカルのまま保つ。全体をクラウド前提へ作り替えない。決定と理由は `SPEC.md` と PR 本文へ残す。

#### 開発作業で使ってよいもの（製品への同梱とは別）

上の規則は**製品が実行時に何を呼ぶか**の話であり、開発する側の道具立てを縛らない。**開発作業では、サブスクリプションに含まれる範囲の Claude / ChatGPT / Gemini を使ってよい。従量課金のAPI利用はしない**（トークン単価で課金される経路へ作業を流さない）。具体的には次のとおり。

- **ブラウザの Gemini など画像生成・編集サービス** — 検証用の素材作り、比較対象の作成、構図・ポーズ・局所修正に使ってよい。利用後に、送信した素材・送信先・目的を報告する。**関係のないプロジェクトファイルを送信しない。** 元画像と承認済み成果物を上書きせず、候補を別名で残す
- **Codex / Claude CLI などのエージェント** — ただし互いを自動起動しない（後述「Git・PR 運用」）
- **ComfyUI / Stable Diffusion のローカル環境** — 検証にも、製品のバックエンド候補としても使ってよい

**これらを製品の実行経路へ持ち込まない。** 開発機のブラウザ、開発者のAPIキー、手元の ComfyUI に依存する実装は、利用者の環境で動かない。

**クラウド画像生成は「目標品質の基準」としてのみ使う。** ローカルモデルの出力を評価するとき、同じ入力をクラウドへ通した結果を比較対象に置いてよい。ただしその出力を製品へ同梱せず、生成物をキャラクター資産として出荷しない。

#### 画像生成バックエンドの選択肢

製品側の画像生成・編集は次のどちらでもよい。採否は実測品質で決め、結論を [SPEC.md](SPEC.md) へ書く。

| 方式 | 利点 | 欠点 |
|---|---|---|
| `stable-diffusion.cpp`（GGUF） | 配布が軽い。Python 不要。Rust から直接叩ける | 対応するモデル・拡張が限られる |
| **ComfyUI 同梱** | ControlNet・IP-Adapter・インペイント・各種最新モデルがそのまま使える。**表情生成の品質リスク（最大の技術リスク）を大きく下げられる** | 配布サイズが増える。Python が画像経路にも入る。ワークフロー JSON の保守が必要 |

ComfyUI を採る場合も、**利用者に ComfyUI の別途インストールを要求しない**。同梱して本アプリ管理下で起動し、利用者の既存 ComfyUI 環境を書き換えない。

### リバース資料の扱い

- `docs/REVERSE.md` は本家バイナリの静的解析記録であり、**実装の足場としてのみ存在する**。
- **対応する機能の実装が完了し、参照の必要がなくなった節から順に削除する。** 節を消したら、その内容のうち恒久的に必要な事実（データ形式・パラメータの意味など）は `SPEC.md` へ移してから消す。二重管理しない。
- すべての節が不要になった時点で `docs/REVERSE.md` ごと削除し、`AGENTS.md` / `SPEC.md` / `README.md` からの参照リンクも同じ PR で取り除く。
- 削除した節と、削除してよいと判断した根拠（対応する実装とテスト）は、その PR 本文へ残す。
- **本家のコード・アセット・テクスチャ・モデル・音声・プロンプト文言をリポジトリへ持ち込まない。** 解析対象のバイナリ、展開物、逆コンパイル結果をコミットしない。`.gitignore` で除外する。参照するのは動作仕様とデータ構造の理解までとし、実装は独自に書く。

### 生成パイプラインの工程順

- 生成は「背景除去 → 画像→3D → リギング → 中立キャプチャ → 表情生成 → 逆投影」の順で、**前段の成果物をディスクへ保存してから次へ進む**。工程順を誤ると成果物が壊れるため、途中工程だけの再実行を必ず可能にする。
- **入力イラストの原本（`source/input.png`）は上書き・削除しない。** すべての再生成はここを起点にする。
- フェイスパッチのキャッシュは署名で判定する。**署名に含める値を増やしたら版番号を上げる。** 古いキャッシュを新しいパイプラインで使い回さない。
- 生成に失敗したら**明示エラーにして理由を利用者へ見せる**。崩れたモデルや空のテクスチャを黙って出さない。

### 画像の扱い

- **画像の引き伸ばし拡大は禁止する。** nearest / bilinear / bicubic / Lanczos 等の補間による拡大結果を、高精細素材や最終レイヤーへ採用しない。
- **透過キャラクターの成果物に、背景の消し残しと誤認されるハロー・矩形・色面・太い外周影を付けない。** 四隅のアルファ値だけで合格にせず、透過市松または透明な画面で実表示を確認する。
- 逆投影で生成したテクスチャは、**書き込み画素数と書き込み領域を必ず検証する**。0画素なら失敗として扱う。

## エラー処理・リソース

- **実害のあるバグは見つけ次第その場で直す。** 別の作業中に見つけた場合も「今回の目的外」で保留にしない。実害とは、利用者のデータが消える・機能が黙って動かなくなる・誤った内容を見せる・費用や時間を無駄にさせること。
- **利用者から見える失敗を、ログだけで済ませない。** ログは利用者が見る場所ではない。数分かかる生成の失敗は必ず画面へ出す。
- **エラーは原則 `Result` で上位へ伝播させる。サイレントスキップ・無言フォールバック・無言デフォルト降格は禁止。** フォールバックを書く場合はその箇所に `// フォールバック許可: 理由` コメントを付ける。
  - 例外として、外部プロセスの起動可否を返すプローブは、未起動・タイムアウトを `false` に正規化してよい。
- `unwrap()` / `expect()` は、起動時の致命的初期化や Mutex ロックなど「失敗＝即異常終了が妥当」な箇所に限る。通常経路では `?` と `Result` を使う。
- **取得したリソースは明示的に解放する。** 子プロセス・ファイルハンドル・ネットワーク接続・タイマー・イベントリスナ・フロントの `URL.createObjectURL` / WebGL コンテキストなど、確保したものは正常終了・失敗・中断のどの経路でも解放する。
  - **子プロセスは終了時に `kill` し、`wait` でハンドルまで回収する。** Python サイドカーと推論エンジンはゾンビになりやすい。
  - **プロセスを止める前に、必ず親プロセスとコマンドラインで「自分が起動したもの」かを確かめる。** 名前だけで一括終了しない。
  - three.js のジオメトリ・マテリアル・テクスチャは `dispose()` する。キャラ切り替えで確実にリークする箇所なので必ず通す。
- **状態ファイル（設定・キャラ一覧・履歴など）の書き込みは原子的に行う。** 一時ファイル → `sync_all` → `rename`。直接上書きすると書き込み中の異常終了で壊れる。
- **「壊れていたら空で継続」するフォールバックを書いたら、その空データをそのまま保存し返さないこと。** 破損時に空で開始し次の保存で書き戻すと、利用者のデータが恒久的に消える。読めなかったファイルは退避してから空で継続する。
- **文字列の切り詰めはバイト添字で行わない。** Rust の文字列は UTF-8 なので、位置が文字の途中だと panic する。日本語は 1 文字 3 バイトのため固定長で切ればほぼ必ず境界を外す。
- **ストリーミング受信のバイト列は、チャンクごとに `String::from_utf8_lossy` しない。** HTTP のチャンク境界は文字の途中に落ちるため、日本語が文字化けする。

## 設定値・シークレット

- 設定の正本は `src-tauri/src/config.rs`（永続ファイル `app_config_dir/config.json`）。優先順位は **永続ファイル > 環境変数 > ハードコード既定値**。新規設定で環境変数を正本・保存先にしない。
- 数値・閾値・パス・モデル名など「設定として変えうる値」はソースに散らさず、設定から都度読む。フェイスパッチの投影パラメータ、リップシンクのフォルマント値、生成の解像度はすべてこれに該当する。
- **設定キー一覧の正本は [docs/SETTINGS.md](docs/SETTINGS.md)。** 記載漏れ・削除漏れを実装と双方向で検出するテストを置き、手作業の突き合わせを不要にする。
- 本実装はクラウドAPIを使わないため **API キー設定を持たない**。将来追加する場合は、`type="password"` での申告、キー名への `API_KEY` / `TOKEN` / `SECRET` / `PASSWORD` / `CREDENTIAL` の付与、OS 資格情報ストアへの保存（平文で `config.json` に置かない）、ログ・エラーメッセージ・ドキュメントへ値を出さないことを必須とする。

## 外部依存・同梱物

- **外部依存は原則として公式 LTS または長期保守安定版を採用し、LTS 相当が無ければ安定版 latest を採用する。** 推論エンジン（llama.cpp / whisper.cpp / stable-diffusion.cpp）とモデルは公式の最新安定版へ追従する。
- **CUDA を使う Python ランタイムは、Python 単体の最新版ではなく、対象 GPU・CUDA・PyTorch・CUDA拡張の公式 wheel が同時に対応する安定版へ統一する。現在の基準は Python 3.12 とする。**
- **大容量のモデル・ランタイム・GPU 向けパッケージは、機能の利用意思と OS・CPUアーキテクチャ・GPU種別・必要能力を取得前に判定し、利用できない物を予備またはフォールバック目的で取得しない。** CUDA 版は CUDA 対応 NVIDIA GPU を確認できた場合だけ取得する。
- **モデル重みの商用利用条項・地域制限・再配布可否を、採用前に必ず確認して結論を `SPEC.md` へ書く。** ライセンス不適合なら候補を差し替える。確認せずに実装へ入らない。
- 同梱実行ファイル・同梱モデル・同梱ランタイムは本アプリ管理下の専用ディレクトリへ設置し、既存の手動インストール環境を書き換えない。実行時データは `app_data_dir` 配下へ置く。
- **取得・更新・ビルドの正常終了後は、旧版・退避・staging・`.part`・完了済み再開キャッシュを残さない。** 新版の検証が成功してから旧版を削除し、失敗時は復旧に必要な物だけを残す。
- 推論エンジン本体とモデルファイルはリポジトリに含めない（`.gitignore` 済み、セットアップタスクで取得する）。
- 依存を変更したら `cargo audit` で既知脆弱性を確認する。

## Git・PR 運用

- **すべての変更は PR で行い、`main` へ直接 push しない。** 作業開始時に `origin/main` から目的別ブランチを作り、同じ目的の変更だけを1つの PR へまとめる。無関係な修正は別ブランチ・別 PR へ分ける。
- **PR 本文を変更記録とレビュー記録の正本にする。** 目的、主な変更、採否と理由、実施した検証、未確認事項をその PR へ書く。完了した作業を別の Markdown 台帳へ複写しない。
- **PR 本文には、別のエージェントが会話履歴なしで引き継げる改修設計を書く。** クレジットの枯渇、セッション切断、担当交代はいつでも起きる。そのとき残るのは PR 本文とコードだけで、会話は残らない。**「続きをやって」と言われた別 AI が、PR 本文だけを読んで同じ判断へ到達できること**を基準にする。
  - **背景と原因**: 何が問題で、なぜそうなっていたか。「直した」ではなく「何が壊れていて、どの経路で起きるか」を書く。
  - **設計判断と却下案**: 採った方式と、**採らなかった方式とその理由**。理由が消えると、引き継いだ側が同じ検討をやり直すか却下済みの案へ戻る。
  - **未完了の作業**: この PR で終わらなかった部分、次にやること、着手条件。条件待ちなら `docs/TASKS.md` へも書く。
  - **検証の状態**: 実行した検証と結果、**この環境では確認できなかったこと**（実機・GUI・GPU・生成の実走が要るもの）を分けて書く。引き継いだ側が「確認済み」と誤解しないようにする。
  - **触ってはいけない前提**: その変更が依存している不変条件（工程順、キャッシュ署名、ロックの取り方、他モジュールとの約束）。知らずに壊されやすいものを名指しする。
  - 作業を途中で終える場合は、PR 本文の冒頭へ「未完了。次にやること」を置き、Draft のままにする。ブランチと変更は必ず push してから終える。**ローカルにしか無い作業は引き継げない。**
- **利用者が設計レビューを行う作業は、実装より先に Draft PR を作る。** 作業エージェントはコードへ着手せず、PR 本文へ背景・要件・採用案・却下案と理由・変更対象と不変条件・検証計画・未確認事項・レビューで判断してほしい論点を書く。
- **Codex と Claude は互いを自動起動しない。** 相互レビューは利用者の操作だけをトリガーとする。利用者が対象成果物・レビュー担当・範囲を明示した場合だけ実行し、その指示の有効範囲は指定された同一 PR 内だけとする。
- **PR のマージは squash merge を使う。** 1 PR = 1 コミットになり `main` の履歴が読みやすいため。
- **作業ブランチを残さない。** マージまで終えたら、その場でリモートとローカルの両方を消す。`gh pr merge --squash --delete-branch` はリモートしか消さないので、ローカルは `git switch main && git pull --ff-only` のあとに `git branch -d` まで行う。
  - **squash merge したブランチは `git branch --merged` で検出できない。** 判定はブランチ名ではなく、**その枝の中身が現行 `main` に存在するか**で行う。
  - **中身が `main` に無い枝を、確認せずに消さない。逆に、古い枝を確認せずにマージもしない。**
- **修正・作業が完了したら、利用者からの個別指示がなくても `main` へのマージまで終わらせる。** 手順は「`git add -A` → コミット → 作業ブランチへ push → PR 作成 → squash merge」。ただし**設計レビュー目的の Draft PR は、利用者の承認があるまでマージしない**。

## スキル

- 作業エージェント向けの再利用手順は `.agents/skills/<名前>/SKILL.md` を正本にする。Claude Code 用の `.claude/skills/<名前>/SKILL.md` は同名の共通スキルを読むだけの薄い入口とし、手順を複製しない。
- **同じ種類の作業が2回以上発生した、工程順を誤ると成果物が壊れる、混入・公開・破壊の危険がある、または複数エージェントへ引き継ぐ作業は、その作業内で共通スキル化する。** 生成パイプラインの実行、実画面検証、依存監査、配布ビルドはこの対象とする。単発の小修正はスキル化しない。
- 新規作成前に `.agents/skills/` を検索し、目的が重なるスキルがあれば新設せず既存スキルへ統合する。
- スキルを使うたびに、記載されたパス・コマンド・版・成果物を現行コード・文書と照合する。古い情報や不足を見つけたら、そのスキルを同じ PR で更新してから作業を続け、過去の手順をそのまま実行しない。
- スキル名は英小文字・数字・ハイフンだけの動詞始まりとし、`SKILL.md` の frontmatter は `name` と「何をするか・どの依頼で使うか」を含む `description` だけにする。本文は日本語、命令形、500行未満を目安とし、一般論や作業日記を入れない。
- 壊れやすい工程は、正規入口・入力条件・禁止経路・成果物の識別方法・失敗時の停止条件・検証まで書く。単なるコマンド一覧にしない。

## 開発環境

- 対応 OS は **Windows 10/11 x64 を先行**。macOS / Linux は後続。
- **Rust（cargo）と Tauri CLI**、Windows は WebView2 ランタイムが必要。**Node.js はアプリとビルドの依存にしない**（作業用ツールとしての利用は可）。判断基準は「アプリのビルド・起動・配布に Node が要るようになるか」。
- 開発起動・ビルド・セットアップは `cargo xtask` に集約する。手順は [docs/DEVELOPMENT.md](docs/DEVELOPMENT.md)。
- **開発・検証で PowerShell を使う場合は PowerShell 7 の `pwsh` を既定にする。** 見つからなければ Windows PowerShell 5.1 へ黙って降格せず、導入が必要な理由を利用者へ伝える。
- 起動して確認したら使い終わったプロセスは止め、ロックや再ビルドの無駄を避ける。確認が自動化しづらい（GUI・音声・画質など）場合は、コードの正しさで判断し、最終目視は利用者に委ねてよい。
