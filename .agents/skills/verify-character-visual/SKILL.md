---
name: verify-character-visual
description: 生成キャラクターの見た目を実画面で検証する。2.5Dパーツの継ぎ目・透過・重なり、まばたき、MouthOpenYとMouthForm、モーションを確認する依頼で使う。旧3Dフェイスパッチを再検証するときにも使う。
---

# キャラクターの見た目を実画面で検証する

2.5Dのパーツ分解とリップシンクは**数値が正しくても見た目が破綻する**種類の処理。テストが通ったことを完了条件にしない。

## 先に数値で見る

実画面の確認は高コストなので、先に安い検査を通す。

- 原画と分解後PSDのキャンバス寸法が一致するか
- 閉口、開口、左右の目、顔、前後髪、胴体の必須パーツが揃っているか
- 各パーツの切詰め領域とアルファチャンネルが期待どおりか

ここで異常が出たら実画面を見る前に直す。

## 透過を確認する

**四隅のアルファ値だけで合格にしない。** 透過市松または透明な画面で実表示を確認する。

見るもの:

- **ハロー**（輪郭の白or黒フチ）— 背景除去の消し残しと誤認される。出ていたら不合格
- **矩形の縁**（切り出し範囲の直線が見える）
- **色面**（本来透明な領域にうっすら色が乗る）
- **太い外周影**

意図した背景が必要な場合は、キャラクター画像側に焼き込まず**画面側の明示的な背景レイヤー**で出す。

## 2.5Dの重なりと変形を確認する

- 目・口の開閉で顔の穴や下地が露出しないか
- 目の開き1/0.5/0を左右別々に比較し、半閉眼の二重線・列の段差、閉眼時の元まつげ/目尻の残留、髪の欠けがないか
- 前髪、横髪、後髪、顔、首、胴体の前後関係が正しいか
- 小角度の顔・体移動でパーツの継ぎ目が開かないか
- 腕を体へ寄せても袖や胴体を不自然に横切らないか
- 原画と並べ、顔立ち・髪・衣装・全体比率が変わっていないか
- 原寸合成だけでなく、縮小・拡大・半画素の位置ずれでも内部分割境界のアルファが薄くならないか。254等の半透明を含む原画で、髪際・顔・首の市松漏れを確認する。

## 旧3Dフェイスパッチを確認する場合

生成直後に必ず見る。破綻パターンは決まっている。

| 症状 | 原因 |
|---|---|
| 顔が**後頭部や髪の裏**にも出ている | レイキャストの最前面判定が効いていない |
| 顔が**体や別パーツ**に出ている | 頭ボーンのウェイト閾値で三角形を絞れていない |
| 顔が**ずれている・縮んでいる** | キャプチャフレームとメッシュのポーズが不一致。中立スナップショットを使っていない可能性 |
| 顔に**細い隙間の格子**が見える | UV継ぎ目の膨張が足りない |
| 顔の周囲に**他パーツの色が滲む** | 継ぎ目膨張が非採用三角形のUV領域を避けていない |
| **表情が変わって見えない** | 差分マスクが厳しすぎる、または投影画素がほぼ0 |
| **顔全体が変わってしまう**（髪型・色が違う） | 画像生成側のドリフト。差分マスクが緩い |
| **左右が反転している** | 画像生成側のミラーリング。プロンプトの反転禁止が効いていない |

正面だけでなく**斜め・横からも見る**。正面だけだと貫通と位置ずれを見落とす。

## 表情と母音の切り替わりを確認する

- 6表情がそれぞれ区別できるか
- **母音が区別できるか。** 特に `i` と `e`、`u` と `o` は潰れやすい
- 切り替わりに引っかかりや一瞬の白抜けが無いか
- 無音時に口が閉じるか

リップシンクは**合成した母音音声（既知のフォルマント）で先に検査**し、実マイクでの確認は最後に1回でよい。

## ブラウザ確認画面を確認する

- ブラウザで開いて**背景が透過**しているか（`body` に不透明な背景が付いていないか）
- 状態更新（目開閉・口形）が反映されるか
- 口パクは開口の静止画だけで済ませず、テストを開始して閉口を含む一巡の複数時点で実描画を確認する。待機動作と口パクは別ボタンである。「口パク動作テスト（無音）」と「自動まばたきを開始」が停止表示へ変わり、口と目のスライダー値および実canvasが変化することを確認する。利用者へ動作中として渡す場合は、そのテストが実際に再生中か確認する。
- 動作をスマホへ渡す場合は`tools/capture-character-gallery.mjs <id> <temp内出力先> --video`で実canvasを録画する。DEVELOPMENT.mdに従い、目・口のパラメータ変化とアニメーションを許可したcanvasの複数フレーム差を検査し、要求fpsと実測を混同しない。Playwrightの既定のアニメーション停止状態で同一フレームを取得し、動作不良と誤判定しない。GIFへの縮小は確認用だけとし、色数・透過が変わるため原寸素材や透明境界の合格根拠にしない。
- ウィンドウサイズを変えて描画が追従するか
- 本体プレビューと確認ページで**同じ描画コードを共有**しているか。二重実装になっていないか
- 読込中に別キャラへ切り替えたとき、古い読込の完了や失敗が新しい選択の表示を上書きしないか。`tools/test-avatar-lifecycle.mjs`で世代競合を先に検査する。
- 既存3体の切替・資源解放を`node tools/check-character-switch-memory.mjs <ID1,ID2,ID3> <新しいtemp出力先>`で2巡確認する。DEVELOPMENT.mdの条件に従い、生の取得エラーと旧形式の想定互換を分けて保存する。旧Blob残留・表示失敗は止めて調べる。V8ヒープ・DOM数・revoke回数をOS RAMやVRAMの測定、長時間の漏れなし証明へ拡張しない。
- 必須部位の欠損と描画中の例外が画面へ通知され、正常キャラの再選択で復帰するか。静止した最終フレームを正常動作と扱わない。

OBSとPicoAgentへの実組み込みは対象外とする。変更時はURLに検証版を付け、古いモジュールのキャッシュを見ていないか確認する。実写・女性A・むぎを同じ条件で検証し、失敗した入力も報告する。

## 後始末

- 確認用に起動したプロセス・ブラウザ・推論エンジンは止める
- **プロセスを止める前に、親プロセスとコマンドラインで「自分が起動したもの」か確かめる。** 名前だけで一括終了しない
- 撮影した画像・連番フレームはまず `temp/` にだけ置き、作業途中の証跡をコミットしない。利用者がGit経由のスマホ確認を依頼した場合は、実際に目視した画像だけを公開用成果物として選別し、READMEから辿れるギャラリーへ保存してよい。撮影条件と未達点を併記し、未確認画像を合格例として公開しない。
- three.js のジオメトリ・マテリアル・テクスチャを `dispose()` しているか、キャラ切り替えを数回繰り返してメモリが増え続けないか確認する

## 報告

見た目の最終判断は利用者に委ねてよい。ただし**何をどの角度で確認し、どの症状が出ていないと言えるか**を具体的に書く。「問題なし」だけで済ませない。
