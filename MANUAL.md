# MANUAL.md — PicoVTuber の使い方

概要と導入は [README.md](README.md)、全設定キーは [docs/SETTINGS.md](docs/SETTINGS.md)。ここには一連の利用手順だけを書く。

## 1. モデルを作る

### 1.1 用意するイラスト

| 条件 | 理由 |
|---|---|
| 全身が入っていること | 脚のボーンを置く位置が決まらないため、上半身だけの絵からは歩けるモデルを作れない |
| 正面を向いた直立に近い姿勢 | 側面・背面の推定が姿勢のねじれに弱い。極端なポーズは形が崩れる |
| 背景が透過（PNG）、または単色 | 背景が残ると体の輪郭と誤認する |
| 長辺 1024 px 以上 | テクスチャの解像度が足りないと、拡大で誤魔化さない方針のため甘い仕上がりになる |
| **自分が権利を持つ、または許諾を得た絵** | 取り込み時に確認します。確認なしには生成へ進めません |

### 1.2 取り込みから生成まで

1. 生成ウィザードを開き、イラストを選ぶ。
2. 権利の確認に答える。ここで答えた内容は、できあがるVRMの利用許諾メタデータへ入ります。
3. 「生成を始める」を押す。8つの工程が順に走ります。

| 順 | 工程 | 何をするか | GPU |
|---|---|---|---|
| 1 | 下ごしらえ | 余白を切り、向きを揃え、等倍以下に整える | 不要 |
| 2 | パーツ分割 | 頭・髪・胴・腕・脚のマスクを作る | 任意 |
| 3 | 多視点生成 | 正面から側面・背面を起こす | 推奨 |
| 4 | メッシュ化 | 多視点から素体メッシュとテクスチャを作る | 推奨 |
| 5 | ボーン設定 | VRM humanoid の標準ボーンを入れてスキニングする | 不要 |
| 6 | 表情生成 | 喜／怒／驚／悲／楽のブレンドシェイプを作る | 不要 |
| 7 | 口形生成 | あ・い・う・え・お・ん の6形を作る | 不要 |
| 8 | 書き出し | VRM 1.0 として検証して保存する | 不要 |

途中でアプリを閉じても、次に起動したとき**未完了の工程から再開します**。最初からやり直しにはなりません。

### 1.3 途中で止まったとき

工程が失敗したら、理由がそのまま画面に出ます。よくある原因は次のとおりです。

| 表示 | 原因 | 対処 |
|---|---|---|
| 「生成モデルの重みがありません」 | 機械学習を使う工程の重みが未取得 | `cargo xtask setup models`（製品版は設定画面の「モデルを取得」） |
| 「GPUを利用できません」 | CUDA対応GPUが見つからない | 設定で実行デバイスを `cpu` にする。時間は大きく伸びます |
| 「体の輪郭を取れませんでした」 | 背景が残っている／全身が入っていない | 1.1 の条件で用意し直す |
| 「テクスチャ解像度が足りません」 | 元絵が小さい | より大きい元絵を使う。**引き伸ばしでは通しません** |

**失敗を白紙や正面の複製で埋めて「成功」にはしません。** 配信本番で初めて気づく作りにしないためです。

## 2. 配信する

### 2.1 モデルを読み込む

配信モードでモデルを選びます。生成したモデルのほか、手元にある VRM 1.0 ファイルも読み込めます。

### 2.2 マイクを繋ぐ（リップシンク）

1. 設定画面で入力デバイスを選ぶ（既定は OS の既定入力）。
2. 配信モードで「入力開始」。話すと口が動きます。

**音声はPC内だけで処理し、保存も送信もしません。** 解析用のバッファは使い終えたら捨てます。

口の動きが合わないときは設定を調整します。

| 症状 | 調整する設定 |
|---|---|
| 黙っていても口が動く | `PICOVTUBER_LIPSYNC_SILENCE_RMS` を上げる |
| 話しても口が開かない | `PICOVTUBER_LIPSYNC_SILENCE_RMS` を下げる、`PICOVTUBER_LIPSYNC_GAIN` を上げる |
| 口がガタつく | `PICOVTUBER_LIPSYNC_SMOOTHING` を上げる |
| 口の動きが遅れる | `PICOVTUBER_LIPSYNC_SMOOTHING` を下げる、`PICOVTUBER_LIPSYNC_FRAME_MS` を下げる |

### 2.3 表情を切り替える

- 手動: 配信ビューのプリセットボタン（喜／怒／驚／悲／楽）。
- 自動: 設定で「表情の自動切替」を有効にすると、ローカルAIが話の内容から表情を選びます。**音声認識モデルと小型LLMの両方が導入済みのときだけ有効になります。** 片方でも無ければ「利用不可」と表示され、クラウドサービスでは代替しません。

切り替えは一瞬ではなく、`PICOVTUBER_EXPRESSION_BLEND_MS` の時間をかけて滑らかに変わります。

### 2.4 配信ソフトへ出す

| 出力 | 使い方 | 対応OS |
|---|---|---|
| 透過ウィンドウ | 専用の背景透過ウィンドウが出ます。OBS の「ウィンドウキャプチャ」でこのウィンドウを選んでください | Windows / macOS / Linux |
| 仮想カメラ | OBS の「映像キャプチャデバイス」から PicoVTuber を選んでください | Windows / macOS |

その環境で使えない出力は、理由付きで無効表示になります。**選べるのに何も起きない状態にはしません。**

透過ウィンドウを OBS で取り込むときは、ソースのプロパティで「クライアント領域」をオンに、キャプチャ方法を「Windows 10 (1903以降)」にすると背景の透過が保たれます。

## 3. 困ったとき

| 症状 | 確認すること |
|---|---|
| 起動しても何も出ない | `ui-dist/` が生成されているか（開発時は `cargo xtask build-ui`） |
| 2つ目を起動しても増えない | 仕様です。PicoVTuber は同時に1つしか動きません（設定とモデル一覧の破損を防ぐため）。2つ目の起動は既存ウィンドウを前面に出します |
| 設定が保存されない | 画面に理由が出ます。設定ファイルが壊れていた場合は隣へ退避され、内容は残っています |
| 生成が遅い | 設定の実行デバイスと、GPUを使う工程が実際にGPUで動いているかを確認してください |

ログの置き場所は設定画面の「診断」から開けます。**ログには画像そのものや音声そのものは記録しません**（工程名・所要時間・寸法・失敗理由だけ）。
